# 🚀 Деплой Next.js приложения на TimeWeb Cloud

## 📋 Последние изменения (Февраль 2026)

### Что было изменено:

1. **Удален Dockerfile** - используем нативный деплой Next.js от TimeWeb Cloud
2. **Убран standalone режим** из `next.config.mjs` - не нужен без Docker
3. **Добавлен `export const dynamic = 'force-dynamic'`** во все API роуты для корректной работы
4. **Создан `.env.example`** с необходимыми переменными окружения

### Почему белый экран?

Если вы видите белый экран на всех страницах, это означает:

1. ❌ **База данных не подключена** - проверьте `DATABASE_URL`
2. ❌ **Не выполнены миграции Prisma** - база данных пустая
3. ❌ **Переменные окружения не настроены** в TimeWeb Cloud
4. ❌ **Ошибки при билде** - проверьте логи

## ✅ Пошаговая инструкция по деплою:

## 🚀 Что делать:

### Шаг 1: Настройте базу данных PostgreSQL

**На TimeWeb Cloud:**
1. Зайдите в панель управления
2. Создайте PostgreSQL базу данных (если еще не создана)
3. Скопируйте **DATABASE_URL** (строка подключения)

**Формат:** `postgresql://user:password@host:port/database?schema=public`

### Шаг 2: Настройте переменные окружения в TimeWeb Cloud

В разделе **App Platform → Settings → Environment Variables** добавьте:

```bash
# ОБЯЗАТЕЛЬНЫЕ переменные
DATABASE_URL=postgresql://user:password@host:port/database

# Memberstack (для авторизации)
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY=pk_sb_...
MEMBERSTACK_SECRET_KEY=sk_sb_...

# SendGrid (для email, опционально)
SENDGRID_API_KEY=SG...
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# App URL
NEXT_PUBLIC_APP_URL=https://your-domain.timeweb.cloud

# Node Environment
NODE_ENV=production
```

⚠️ **ВАЖНО:** Без `DATABASE_URL` приложение не запустится!

### Шаг 3: Выполните миграции Prisma

После деплоя выполните миграции через SSH или через раздел "Commands" в TimeWeb Cloud:

```bash
# Применить миграции
npx prisma migrate deploy

# Сгенерировать Prisma Client
npx prisma generate

# (Опционально) Заполнить базу тестовыми данными
npx prisma db seed
```

### Шаг 4: Пересоберите и задеплойте приложение

1. Откройте панель TimeWeb Cloud
2. Перейдите в раздел с вашим приложением (App Platform)
3. Нажмите **"Rebuild"** или **"Пересобрать"**
4. Дождитесь завершения сборки (3-5 минут)

### Шаг 5: Проверьте логи билда

В логах билда должно быть:
```
✓ Compiled successfully
✓ Linting and checking validity of types
✓ Collecting page data
✓ Generating static pages (23/23)
✓ Finalizing page optimization
✓ Build succeeded
```

⚠️ Если видите ошибки `Dynamic server usage: Route ... couldn't be rendered statically` - это нормально! Мы добавили `export const dynamic = 'force-dynamic'` во все API роуты.

### Шаг 6: Проверьте работу сайта

1. Откройте ваш сайт в браузере: `https://your-app.timeweb.cloud`
2. Нажмите **F12** (Developer Tools)
3. Перейдите на вкладку **Console**
4. Обновите страницу

**✅ Что должно работать:**
- Главная страница загружается с дизайном
- Страница `/courses` показывает список курсов
- Нет ошибок в консоли
- CSS и JavaScript загружаются корректно

**❌ Если все еще белый экран:**
- Проверьте логи приложения в TimeWeb Cloud (Runtime Logs)
- Убедитесь что `DATABASE_URL` настроен правильно
- Проверьте что миграции Prisma выполнены

## 🛠️ Проверка подключения к базе данных

Проверьте что приложение может подключиться к PostgreSQL:

1. В логах приложения (Runtime Logs) не должно быть ошибок:
   - ❌ `Can't reach database server`
   - ❌ `Connection timeout`
   - ❌ `Invalid database URL`

2. Если есть ошибки подключения:
   - Проверьте формат `DATABASE_URL`
   - Убедитесь что PostgreSQL сервер запущен
   - Проверьте firewall правила (разрешен ли доступ из App Platform)

## 🧪 Локальное тестирование

Перед деплоем протестируйте локально:

```bash
# 1. Установите зависимости
npm install

# 2. Настройте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте свой DATABASE_URL

# 3. Выполните миграции
npx prisma migrate dev

# 4. (Опционально) Заполните тестовыми данными
npx prisma db seed

# 5. Соберите приложение
npm run build

# 6. Запустите production сервер
npm start

# 7. Откройте http://localhost:3000
```

Если локально все работает, значит проблема в конфигурации TimeWeb Cloud.

## 🔍 Отладка распространенных проблем

### 1. Белый экран (нет контента)

**Причина:** База данных не подключена или пустая

**Решение:**
```bash
# Проверьте DATABASE_URL в переменных окружения
# Выполните миграции:
npx prisma migrate deploy
npx prisma db seed
```

### 2. Ошибка "Can't reach database server"

**Причина:** Неправильный DATABASE_URL или firewall

**Решение:**
- Проверьте формат DATABASE_URL
- Убедитесь что PostgreSQL доступен из App Platform
- Проверьте whitelist IP адресов в настройках базы данных

### 3. API роуты возвращают 500 ошибку

**Причина:** Prisma Client не сгенерирован или старая версия

**Решение:**
```bash
npx prisma generate
```

### 4. Статические файлы не загружаются (404)

**Причина:** Проблемы с Next.js build

**Решение:**
- Проверьте что `next build` завершился успешно
- Убедитесь что `output: 'standalone'` УБРАН из next.config.mjs
- Проверьте логи билда на наличие ошибок

### 5. Проверьте логи в реальном времени

**В TimeWeb Cloud:**
1. App Platform → Ваше приложение
2. Вкладка **Logs** или **Runtime Logs**
3. Ищите ошибки при запуске

**Типичные ошибки:**
- `Error: P1001: Can't reach database server` - проблемы с БД
- `Error: Invalid DATABASE_URL` - неправильный формат строки подключения
- `ECONNREFUSED` - база данных недоступна

## 📝 Итоговые изменения

Что было сделано:
- ❌ **Удален** `Dockerfile` - используем нативный Next.js деплой
- ✅ **Убран** `output: 'standalone'` из `next.config.mjs`
- ✅ **Добавлен** `export const dynamic = 'force-dynamic'` во все 15 API роутов
- ✅ **Создан** `.env.example` с необходимыми переменными
- ✅ **Обновлена** документация по деплою

## 📌 Конфигурация TimeWeb Cloud App Platform

**Рекомендуемые настройки:**

- **Build Command:** `npm run build`
- **Start Command:** `npm start`
- **Node Version:** 18.x или 20.x
- **Port:** 3000 (по умолчанию Next.js)
- **Health Check:** `/` (главная страница)

**Переменные окружения (минимум):**
```
DATABASE_URL=postgresql://...
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://your-domain.timeweb.cloud
```

## ✅ Checklist перед деплоем

Убедитесь что:

- [ ] PostgreSQL база данных создана на TimeWeb Cloud
- [ ] `DATABASE_URL` добавлен в Environment Variables
- [ ] Все необходимые переменные окружения настроены
- [ ] Dockerfile удален (или переименован в Dockerfile.backup)
- [ ] `output: 'standalone'` убран из next.config.mjs
- [ ] Код закоммичен и запушен в репозиторий
- [ ] Миграции Prisma будут выполнены после деплоя

## 🎯 Следующие шаги после успешного деплоя

1. **Настройте кастомный домен** (если есть)
2. **Настройте SSL сертификат** (обычно автоматически)
3. **Настройте Memberstack** для авторизации пользователей
4. **Протестируйте все функции:**
   - Регистрация/авторизация
   - Просмотр курсов
   - Enrollment в курсы
   - Прохождение уроков
   - Получение сертификатов

---

**Проблемы остались?** Проверьте:
1. Runtime Logs в TimeWeb Cloud
2. Database connection status
3. Environment Variables
4. Build logs для ошибок компиляции

# 🚀 Инструкция по деплою

## ✅ Проблема решена!

`ReferenceError: localStorage is not defined` - **ИСПРАВЛЕНО**

Подробное описание исправления: **`LOCALSTORAGE-FIX-FINAL.md`**

---

## 📦 Деплой на Production

### 1. Проверьте локально (опционально)

```bash
# Убедитесь, что build работает
npm run build

# Запустите production сервер
npm start

# Откройте http://localhost:3000 и проверьте страницы
```

### 2. Закоммитьте изменения

```bash
git add .
git commit -m "fix: resolve localStorage SSR error in MemberstackProvider"
git push origin main
```

### 3. TimeWeb автоматически пересоберет

- Зайдите в панель TimeWeb
- Проверьте логи сборки
- ✅ Должно быть: "✓ Generating static pages (15/15)"
- ❌ НЕ должно быть: "ReferenceError: localStorage"

### 4. Проверьте сайт

Откройте ваш сайт и проверьте страницы:
- https://ваш-домен.ru/
- https://ваш-домен.ru/courses
- https://ваш-домен.ru/admin
- https://ваш-домен.ru/dashboard

**В консоли браузера (F12) не должно быть ошибок!**

---

## 🔍 Что было исправлено

**Файл:** `src/components/MemberstackProvider.tsx`

**Проблема:** Статический импорт `@memberstack/dom` вызывал ошибку localStorage во время SSR

**Решение:** Динамический импорт `import('@memberstack/dom')` только на клиенте

**Результат:**
- ✅ Build успешен
- ✅ Все 15 страниц сгенерированы
- ✅ Production ready

---

## ✅ Готово к деплою!

Все исправления применены, приложение полностью готово к production.

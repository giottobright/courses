# 🚀 Полное руководство по деплою на Timeweb Cloud

> **Пошаговая инструкция от первого шага до финального запуска платформы Learnify на Timeweb Cloud**

---

## 📋 Содержание

1. [Подготовка к деплою](#1-подготовка-к-деплою)
2. [Регистрация на Timeweb Cloud](#2-регистрация-на-timeweb-cloud)
3. [Создание PostgreSQL базы данных](#3-создание-postgresql-базы-данных)
4. [Подготовка кода к деплою](#4-подготовка-кода-к-деплою)
5. [Настройка внешних сервисов](#5-настройка-внешних-сервисов)
6. [Создание Node.js приложения](#6-создание-nodejs-приложения)
7. [Деплой кода на сервер](#7-деплой-кода-на-сервер)
8. [Настройка переменных окружения](#8-настройка-переменных-окружения)
9. [Запуск миграций базы данных](#9-запуск-миграций-базы-данных)
10. [Запуск приложения](#10-запуск-приложения)
11. [Настройка домена](#11-настройка-домена)
12. [Финальное тестирование](#12-финальное-тестирование)
13. [Мониторинг и обслуживание](#13-мониторинг-и-обслуживание)
14. [Решение проблем](#14-решение-проблем)

---

## 1. Подготовка к деплою

### 1.1 Что вам понадобится

- [ ] Код проекта Learnify (у вас уже есть)
- [ ] Аккаунт на Timeweb Cloud (создадим)
- [ ] Банковская карта для оплаты хостинга
- [ ] Email для регистрации
- [ ] Git установлен на вашем компьютере
- [ ] SSH клиент (встроен в Windows 10+)

### 1.2 Проверка кода локально

Перед деплоем убедитесь, что проект работает локально:

```bash
# Перейдите в папку проекта
cd c:\Users\User\Desktop\courses

# Установите зависимости (если еще не установлены)
npm install

# Попробуйте собрать production версию
npm run build

# Если сборка прошла успешно - отлично!
```

**Если увидите ошибки при сборке:**
- Исправьте их перед деплоем
- Проверьте TypeScript ошибки
- Убедитесь что все зависимости установлены

### 1.3 Создание Git репозитория (если еще не создан)

```bash
# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit - ready for deployment"
```

**Важно:** Убедитесь что `.env` файл добавлен в `.gitignore` (он уже должен быть там)

---

## 2. Регистрация на Timeweb Cloud

### 2.1 Создание аккаунта

1. **Откройте браузер** и перейдите на https://timeweb.cloud

2. **Нажмите "Регистрация"** (правый верхний угол)

3. **Заполните форму:**
   - Email: ваш email
   - Пароль: придумайте надежный пароль
   - ФИО: ваши данные
   - Телефон: номер телефона

4. **Подтвердите email:**
   - Проверьте почту
   - Кликните на ссылку подтверждения

5. **Войдите в личный кабинет**

### 2.2 Пополнение баланса

1. **Перейдите в раздел "Баланс"** (левое меню)

2. **Нажмите "Пополнить"**

3. **Выберите сумму:**
   - Для начала хватит 1000 рублей
   - База данных: ~300₽/месяц
   - VDS сервер: ~400-800₽/месяц

4. **Оплатите банковской картой**

5. **Дождитесь зачисления** (обычно моментально)

---

## 3. Создание PostgreSQL базы данных

### 3.1 Создание инстанса БД

1. **В панели Timeweb перейдите:**
   ```
   Базы данных → Создать базу данных
   ```

2. **Выберите PostgreSQL:**
   - Тип: PostgreSQL
   - Версия: 15 или новее (рекомендуется последняя)

3. **Выберите конфигурацию:**
   
   **Для начала (тестирование):**
   - CPU: 1 vCPU
   - RAM: 2 GB
   - Disk: 10 GB SSD
   - Цена: ~300₽/месяц
   
   **Для production (рекомендуется):**
   - CPU: 2 vCPU
   - RAM: 4 GB
   - Disk: 20 GB SSD
   - Цена: ~600₽/месяц

4. **Выберите регион:**
   - Выберите ближайший к вашим пользователям
   - Москва или Санкт-Петербург для России

5. **Настройте базу данных:**
   - Имя инстанса: `learnify-db` (любое имя)
   - Пароль администратора: придумайте надежный пароль (сохраните его!)
   - Включите автобэкапы: ДА (важно!)

6. **Нажмите "Создать"**

7. **Дождитесь создания** (5-10 минут)
   - Статус изменится на "Активна"

### 3.2 Получение данных для подключения

После создания базы:

1. **Откройте созданную базу** (кликните на неё)

2. **Найдите раздел "Подключение"**

3. **Скопируйте данные:**
   ```
   Хост: xxx.timeweb.cloud или IP адрес
   Порт: 5432
   Пользователь: postgres (или ваш)
   Пароль: тот что вы создали
   База данных: postgres (по умолчанию)
   ```

4. **Сохраните эти данные** - они понадобятся позже!

### 3.3 Создание базы данных для приложения

1. **Подключитесь к базе через pgAdmin** (если есть) или через веб-интерфейс Timeweb

   Или используйте командную строку:
   ```bash
   # Установите psql клиент (если нет)
   # Для Windows: скачайте с postgresql.org
   
   # Подключитесь
   psql -h ваш-хост.timeweb.cloud -U postgres -d postgres
   ```

2. **Создайте базу данных для Learnify:**
   ```sql
   CREATE DATABASE learnify;
   ```

3. **Проверьте что база создана:**
   ```sql
   \l
   ```
   
   Должна появиться `learnify` в списке

4. **Создайте DATABASE_URL для .env:**
   ```
   DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@ваш-хост.timeweb.cloud:5432/learnify?schema=public"
   ```
   
   **Сохраните эту строку** - она понадобится в шаге 8!

---

## 4. Подготовка кода к деплою

### 4.1 Обновление package.json

Убедитесь что в `package.json` есть правильные скрипты:

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:seed": "prisma db seed"
  }
}
```

**Важно:** Для production используется `prisma migrate deploy` вместо `prisma migrate dev`

### 4.2 Обновление next.config.mjs

Откройте `next.config.mjs` и проверьте настройки:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'api.dicebear.com',
      'res.cloudinary.com', // Для загруженных изображений
      'i.vimeocdn.com', // Для превью видео Vimeo
    ],
  },
  // Для production на Timeweb
  output: 'standalone', // ВАЖНО для деплоя!
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig;
```

**Сохраните файл** если внесли изменения.

### 4.3 Создание production env template

Создайте файл `.env.production.template` для справки:

```env
# DATABASE
DATABASE_URL="postgresql://postgres:PASSWORD@host.timeweb.cloud:5432/learnify?schema=public"

# APP
NEXT_PUBLIC_APP_URL="https://ваш-домен.ru"
NEXT_PUBLIC_APP_NAME="Learnify"

# MEMBERSTACK (получим в шаге 5)
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY="pk_..."
MEMBERSTACK_SECRET_KEY="sk_..."

# STRIPE (через Memberstack)
# Ключи Stripe будут настроены в Memberstack панели

# VIMEO (получим в шаге 5)
VIMEO_ACCESS_TOKEN="..."

# SENDGRID (получим в шаге 5)
SENDGRID_API_KEY="SG...."
SENDGRID_FROM_EMAIL="noreply@ваш-домен.ru"

# CLOUDINARY (получим в шаге 5)
CLOUDINARY_CLOUD_NAME="..."
CLOUDINARY_API_KEY="..."
CLOUDINARY_API_SECRET="..."

# ADMIN (опционально)
NEXT_PUBLIC_ADMIN_USER_IDS="memberstack_user_id_1,memberstack_user_id_2"
```

**Не коммитьте этот файл в Git!**

---

## 5. Настройка внешних сервисов

Теперь настроим все внешние сервисы, которые использует приложение.

### 5.1 Memberstack (Авторизация + Платежи)

#### Шаг 1: Регистрация

1. **Перейдите на** https://www.memberstack.com
2. **Нажмите "Start Free"**
3. **Заполните форму:**
   - Email
   - Company name: "Learnify" (или ваше название)
   - Create password
4. **Подтвердите email**

#### Шаг 2: Создание проекта

1. **После входа нажмите "Create App"**
2. **Выберите:**
   - Platform: "Next.js"
   - Name: "Learnify Production"
3. **Нажмите Create**

#### Шаг 3: Настройка приложения

1. **Перейдите в Settings → General:**
   - App URL: `https://ваш-домен.ru` (пока оставьте временный)
   - Allowed Domains: добавьте ваш домен

2. **Настройте Login/Signup страницы:**
   ```
   Settings → Pages & Redirects:
   - Login page: /login
   - Signup page: /login?signup=true
   - Dashboard redirect: /dashboard
   - Logout redirect: /
   ```

3. **Получите API ключи:**
   ```
   Settings → API & SDKs → Copy Keys
   ```
   
   Сохраните:
   - **Public Key** (начинается с `pk_...`)
   - **Secret Key** (начинается с `sk_...`)

#### Шаг 4: Настройка Stripe в Memberstack

1. **В Memberstack перейдите:**
   ```
   Settings → Payments → Connect Stripe
   ```

2. **Нажмите "Connect with Stripe"**

3. **Создайте аккаунт Stripe** (если нет):
   - Перейдите на stripe.com
   - Зарегистрируйтесь
   - Заполните данные компании
   - Активируйте аккаунт

4. **Подключите Stripe к Memberstack:**
   - Авторизуйтесь в Stripe
   - Разрешите доступ Memberstack
   - Вернитесь в Memberstack

#### Шаг 5: Создание планов (для платных курсов)

1. **В Memberstack перейдите:**
   ```
   Plans → Create Plan
   ```

2. **Создайте планы для примера:**
   
   **План 1: Basic Course ($49)**
   - Name: "Basic Course Access"
   - Price: $49
   - Interval: One-time payment
   - Plan ID: скопируйте (нужен для курсов)

   **План 2: Premium Course ($89)**
   - Name: "Premium Course Access"
   - Price: $89
   - Interval: One-time payment
   - Plan ID: скопируйте

3. **Сохраните Plan IDs** - они нужны для привязки к курсам

### 5.2 Vimeo (Видео хостинг)

#### Шаг 1: Регистрация

1. **Перейдите на** https://vimeo.com
2. **Нажмите "Join"**
3. **Выберите план:**
   - Starter: $20/месяц (подходит для начала)
   - Standard: $33/месяц (больше места)
4. **Зарегистрируйтесь и оплатите**

#### Шаг 2: Получение API токена

1. **Перейдите на** https://developer.vimeo.com/apps

2. **Нажмите "Create App":**
   - App Name: "Learnify Platform"
   - App Description: "Video hosting for online courses"
   - Согласитесь с условиями
   - Нажмите Create

3. **Получите Access Token:**
   ```
   На странице приложения:
   → Authentication → Generate new token
   ```
   
   **Выберите права (scopes):**
   - ✅ Public
   - ✅ Private
   - ✅ Video Files
   - ✅ Upload

4. **Скопируйте токен** (начинается с букв/цифр)
   - **Важно:** Сохраните токен, он больше не покажется!

5. **Сохраните для .env:**
   ```
   VIMEO_ACCESS_TOKEN="ваш_токен_здесь"
   ```

### 5.3 SendGrid (Email рассылка)

#### Шаг 1: Регистрация

1. **Перейдите на** https://sendgrid.com
2. **Нажмите "Start for Free"**
3. **Заполните форму:**
   - Email
   - Password
   - Согласитесь с условиями
4. **Подтвердите email**

#### Шаг 2: Настройка аккаунта

1. **После входа заполните профиль:**
   - First Name, Last Name
   - Company
   - Website: ваш будущий домен
   - Phone number

2. **Выберите:**
   - How many emails?: "Less than 40,000"
   - Email type?: "Transactional"
   - Нажмите Get Started

#### Шаг 3: Создание API ключа

1. **Перейдите:**
   ```
   Settings → API Keys → Create API Key
   ```

2. **Настройте ключ:**
   - API Key Name: "Learnify Production"
   - API Key Permissions: "Full Access" (или Restricted для безопасности)
   - Нажмите Create & View

3. **Скопируйте API ключ:**
   - Начинается с `SG.`
   - **Важно:** Сохраните сразу, больше не покажется!

4. **Сохраните для .env:**
   ```
   SENDGRID_API_KEY="SG.ваш_ключ"
   SENDGRID_FROM_EMAIL="noreply@ваш-домен.ru"
   ```

#### Шаг 4: Верификация домена (опционально, но рекомендуется)

1. **Перейдите:**
   ```
   Settings → Sender Authentication → Domain Authentication
   ```

2. **Нажмите "Authenticate Your Domain"**

3. **Добавьте DNS записи** (получите от SendGrid):
   - Добавьте CNAME записи в настройки вашего домена
   - Дождитесь верификации (может занять до 48 часов)

### 5.4 Cloudinary (Загрузка изображений)

#### Шаг 1: Регистрация

1. **Перейдите на** https://cloudinary.com
2. **Нажмите "Sign Up for Free"**
3. **Заполните форму:**
   - Email
   - Password
   - Или войдите через Google

#### Шаг 2: Получение credentials

1. **После входа откройте Dashboard**

2. **Найдите "Account Details":**
   ```
   Dashboard → Account Details (правый верхний угол)
   ```

3. **Скопируйте:**
   - Cloud Name: (например, `dkxxx123`)
   - API Key: (цифры)
   - API Secret: (буквы и цифры) - нажмите "Show" чтобы увидеть

4. **Сохраните для .env:**
   ```
   CLOUDINARY_CLOUD_NAME="ваш_cloud_name"
   CLOUDINARY_API_KEY="ваш_api_key"
   CLOUDINARY_API_SECRET="ваш_api_secret"
   ```

#### Шаг 3: Настройка upload preset (опционально)

1. **Перейдите:**
   ```
   Settings → Upload → Upload presets
   ```

2. **Создайте preset:**
   - Preset name: "learnify_courses"
   - Signing Mode: "Signed" (для безопасности)
   - Folder: "learnify"

---

## 6. Создание Node.js приложения на Timeweb

### 6.1 Создание VDS сервера

1. **В панели Timeweb перейдите:**
   ```
   Облачные серверы → Создать сервер
   ```

2. **Выберите операционную систему:**
   - ОС: **Ubuntu 22.04 LTS** (рекомендуется)

3. **Выберите конфигурацию:**
   
   **Минимальная (для теста):**
   - CPU: 1 vCPU
   - RAM: 2 GB
   - Disk: 20 GB SSD
   - Цена: ~400₽/месяц
   
   **Рекомендуемая (для production):**
   - CPU: 2 vCPU
   - RAM: 4 GB
   - Disk: 40 GB SSD
   - Цена: ~800₽/месяц

4. **Настройки:**
   - Имя сервера: `learnify-app`
   - SSH ключ: создайте новый или загрузите существующий
   - Пароль root: придумайте надежный пароль

5. **Нажмите "Создать сервер"**

6. **Дождитесь создания** (3-5 минут)

7. **Получите IP адрес** сервера:
   ```
   Откройте созданный сервер → скопируйте IP адрес
   ```

### 6.2 Подключение к серверу по SSH

**На Windows (PowerShell или CMD):**

```bash
# Подключитесь к серверу
ssh root@ВАШ_IP_АДРЕС

# Введите пароль когда попросит
```

**После успешного подключения вы увидите:**
```
root@learnify-app:~#
```

### 6.3 Установка необходимого ПО

**Выполните команды по порядку:**

```bash
# 1. Обновите систему
apt update && apt upgrade -y

# 2. Установите Node.js 20.x (LTS)
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Проверьте установку
node -v  # Должно показать v20.x.x
npm -v   # Должно показать 10.x.x

# 3. Установите Git
apt install -y git

# 4. Установите PM2 (менеджер процессов)
npm install -g pm2

# 5. Установите PostgreSQL клиент (для psql команд)
apt install -y postgresql-client

# 6. Создайте пользователя для приложения (безопасность)
adduser learnify
# Введите пароль и информацию (можно пропустить Enter)

# 7. Добавьте пользователя в sudo группу
usermod -aG sudo learnify

# 8. Переключитесь на нового пользователя
su - learnify
```

**Теперь вы работаете под пользователем `learnify`**

---

## 7. Деплой кода на сервер

### 7.1 Создание Git репозитория (если еще не создан)

**На вашем локальном компьютере:**

#### Вариант A: GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com
   - Нажмите "New repository"
   - Имя: `learnify-platform`
   - Private или Public (выберите Private для безопасности)
   - НЕ добавляйте README (у вас уже есть)
   - Нажмите "Create repository"

2. **Подключите локальный репозиторий:**
   ```bash
   # В вашей папке проекта
   cd c:\Users\User\Desktop\courses
   
   # Добавьте remote
   git remote add origin https://github.com/ваш-username/learnify-platform.git
   
   # Загрузите код
   git branch -M main
   git push -u origin main
   ```

#### Вариант B: GitLab (альтернатива)

Аналогично GitHub, но на gitlab.com

#### Вариант C: Прямая загрузка через SCP (не рекомендуется)

```bash
# Из папки проекта
scp -r . learnify@ВАШ_IP:/home/learnify/learnify-platform
```

### 7.2 Клонирование кода на сервер

**На сервере (под пользователем learnify):**

```bash
# Перейдите в домашнюю папку
cd ~

# Клонируйте репозиторий
git clone https://github.com/ваш-username/learnify-platform.git

# Если репозиторий приватный, введите логин/пароль
# Или используйте Personal Access Token

# Перейдите в папку проекта
cd learnify-platform

# Проверьте что файлы загрузились
ls -la
```

Вы должны увидеть все файлы проекта.

### 7.3 Установка зависимостей

```bash
# В папке проекта
npm install

# Это займет несколько минут
# Дождитесь завершения
```

---

## 8. Настройка переменных окружения

### 8.1 Создание .env файла

**На сервере:**

```bash
# В папке проекта
nano .env
```

### 8.2 Добавление всех переменных

**Скопируйте и вставьте (замените значения на ваши реальные):**

```env
# ========================================
# DATABASE
# ========================================
DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ_БД@ваш-хост.timeweb.cloud:5432/learnify?schema=public&sslmode=require"

# ========================================
# APPLICATION
# ========================================
NEXT_PUBLIC_APP_URL="https://ваш-домен.ru"
NEXT_PUBLIC_APP_NAME="Learnify"
NODE_ENV="production"

# ========================================
# MEMBERSTACK (Authentication)
# ========================================
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY="pk_ваш_публичный_ключ"
MEMBERSTACK_SECRET_KEY="sk_ваш_секретный_ключ"

# ========================================
# STRIPE (через Memberstack - не нужно отдельно)
# ========================================
# Stripe настраивается в панели Memberstack

# ========================================
# VIMEO (Video hosting)
# ========================================
VIMEO_ACCESS_TOKEN="ваш_vimeo_токен"

# ========================================
# SENDGRID (Email)
# ========================================
SENDGRID_API_KEY="SG.ваш_sendgrid_ключ"
SENDGRID_FROM_EMAIL="noreply@ваш-домен.ru"
SENDGRID_FROM_NAME="Learnify Platform"

# ========================================
# CLOUDINARY (Image uploads)
# ========================================
CLOUDINARY_CLOUD_NAME="ваш_cloud_name"
CLOUDINARY_API_KEY="ваш_cloudinary_api_key"
CLOUDINARY_API_SECRET="ваш_cloudinary_api_secret"
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="ваш_cloud_name"

# ========================================
# ADMIN ACCESS (опционально)
# ========================================
NEXT_PUBLIC_ADMIN_USER_IDS=""
# После создания админа в Memberstack, добавьте его ID сюда

# ========================================
# SECURITY (опционально)
# ========================================
# Для production рекомендуется
NEXTAUTH_SECRET="сгенерируйте_случайную_строку_64_символа"
# Сгенерировать можно: openssl rand -base64 32
```

### 8.3 Сохранение файла

```bash
# Нажмите Ctrl + X
# Нажмите Y (да, сохранить)
# Нажмите Enter
```

### 8.4 Проверка .env файла

```bash
# Проверьте что файл создан
ls -la .env

# Посмотрите содержимое (убедитесь что все правильно)
cat .env
```

---

## 9. Запуск миграций базы данных

### 9.1 Генерация Prisma Client

```bash
# В папке проекта
npm run prisma:generate
```

Вы должны увидеть:
```
✔ Generated Prisma Client
```

### 9.2 Применение миграций

```bash
# Создайте таблицы в БД
npx prisma migrate deploy
```

**Что произойдет:**
- Prisma подключится к вашей БД на Timeweb
- Создаст все таблицы из schema.prisma
- Применит миграции

Вы должны увидеть:
```
✔ Applied migrations:
  - 20240xxx_init
  - ...другие миграции
```

### 9.3 Заполнение БД тестовыми данными (опционально)

```bash
# Запустите seed (заполнит демо данными)
npm run prisma:seed
```

**Что добавится:**
- 6-8 демо курсов
- Категории
- Уроки для каждого курса
- Тестовые отзывы

### 9.4 Проверка БД

**Способ 1: Через Prisma Studio**

```bash
# Запустите Prisma Studio
npx prisma studio --port 5555

# В другом терминале создайте SSH туннель
# На вашем локальном компьютере:
ssh -L 5555:localhost:5555 learnify@ВАШ_IP

# Откройте в браузере:
# http://localhost:5555
```

**Способ 2: Через psql**

```bash
# Подключитесь к БД
psql -h ваш-хост.timeweb.cloud -U postgres -d learnify

# Проверьте таблицы
\dt

# Проверьте количество курсов
SELECT COUNT(*) FROM courses;

# Выйдите
\q
```

---

## 10. Запуск приложения

### 10.1 Сборка production версии

```bash
# В папке проекта
npm run build
```

**Это займет 3-5 минут.**

Вы должны увидеть в конце:
```
✓ Compiled successfully
✓ Collecting page data
✓ Generating static pages
✓ Finalizing page optimization
```

**Если увидите ошибки:**
- Проверьте переменные окружения в .env
- Убедитесь что DATABASE_URL правильный
- Проверьте что БД доступна

### 10.2 Тестовый запуск

```bash
# Запустите приложение
npm start
```

Вы должны увидеть:
```
> learnify-platform@1.0.0 start
> next start

- ready started server on 0.0.0.0:3000
```

**Проверьте что работает:**

На вашем компьютере откройте браузер:
```
http://ВАШ_IP:3000
```

**Если не открывается:**
- Проверьте firewall на сервере
- Откройте порт 3000

```bash
# На сервере (Ctrl+C чтобы остановить приложение)
sudo ufw allow 3000
```

Попробуйте снова:
```bash
npm start
# И откройте http://ВАШ_IP:3000
```

**Если сайт открылся - отлично!**

Нажмите **Ctrl+C** чтобы остановить.

### 10.3 Запуск с PM2 (автозапуск и мониторинг)

**PM2** - это менеджер процессов, который:
- Автоматически перезапускает при сбое
- Запускается при перезагрузке сервера
- Показывает логи и статистику

```bash
# Запустите приложение через PM2
pm2 start npm --name "learnify" -- start

# Проверьте статус
pm2 status

# Вы должны увидеть:
# learnify | online | ...
```

### 10.4 Настройка автозапуска

```bash
# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск при перезагрузке
pm2 startup

# Скопируйте и выполните команду которую PM2 покажет
# Например:
sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u learnify --hp /home/learnify
```

### 10.5 Полезные команды PM2

```bash
# Посмотреть логи
pm2 logs learnify

# Посмотреть логи в реальном времени
pm2 logs learnify --lines 100

# Перезапустить приложение
pm2 restart learnify

# Остановить приложение
pm2 stop learnify

# Удалить из PM2
pm2 delete learnify

# Посмотреть детали
pm2 show learnify
```

---

## 11. Настройка домена

### 11.1 Настройка веб-сервера (Nginx)

**Nginx** нужен для:
- SSL сертификат (HTTPS)
- Проксирование запросов на порт 3000
- Кэширование статики

#### Установка Nginx

```bash
# Вернитесь к root пользователю
exit  # выйти из пользователя learnify
# или в новой сессии SSH: ssh root@ВАШ_IP

# Установите Nginx
apt install -y nginx

# Проверьте что запущен
systemctl status nginx

# Запустите если не запущен
systemctl start nginx
systemctl enable nginx
```

#### Создание конфигурации

```bash
# Создайте конфиг для вашего сайта
nano /etc/nginx/sites-available/learnify
```

**Вставьте (замените ваш-домен.ru на реальный домен):**

```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    # Логи
    access_log /var/log/nginx/learnify-access.log;
    error_log /var/log/nginx/learnify-error.log;

    # Проксирование на Next.js
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Кэширование статики
    location /_next/static/ {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
        add_header Cache-Control "public, immutable, max-age=31536000";
    }

    # Кэширование изображений
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp)$ {
        proxy_pass http://localhost:3000;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

**Сохраните:** Ctrl+X → Y → Enter

#### Активация конфигурации

```bash
# Создайте символическую ссылку
ln -s /etc/nginx/sites-available/learnify /etc/nginx/sites-enabled/

# Проверьте конфигурацию
nginx -t

# Должно показать: syntax is ok

# Перезапустите Nginx
systemctl restart nginx
```

### 11.2 Настройка DNS домена

#### Если у вас есть домен (например на reg.ru, timeweb, etc):

1. **Перейдите в панель управления доменом**

2. **Найдите настройки DNS**

3. **Добавьте A записи:**

   ```
   Тип: A
   Имя: @
   Значение: ВАШ_IP_АДРЕС_СЕРВЕРА
   TTL: 3600
   
   Тип: A
   Имя: www
   Значение: ВАШ_IP_АДРЕС_СЕРВЕРА
   TTL: 3600
   ```

4. **Сохраните изменения**

5. **Дождитесь обновления DNS** (до 24 часов, обычно 1-2 часа)

#### Если домена нет:

**Купите домен на:**
- Timeweb (удобно - все в одном месте)
- Reg.ru
- Nic.ru
- Namecheap.com

Цена: ~200-500₽/год для .ru домена

### 11.3 Установка SSL сертификата (HTTPS)

Используем **Let's Encrypt** (бесплатный SSL):

```bash
# Установите Certbot
apt install -y certbot python3-certbot-nginx

# Получите сертификат (замените на ваш домен)
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Следуйте инструкциям:
# 1. Введите email
# 2. Согласитесь с условиями (Y)
# 3. Email рассылка (Y или N)
# 4. Redirect HTTP to HTTPS? → 2 (Redirect)
```

**Certbot автоматически:**
- Получит сертификат
- Настроит Nginx
- Включит HTTPS
- Настроит автообновление

#### Проверка автообновления

```bash
# Проверьте что обновление работает
certbot renew --dry-run

# Должно показать: Congratulations, all simulated renewals succeeded
```

### 11.4 Обновление переменных окружения

```bash
# Переключитесь на пользователя learnify
su - learnify

# Откройте .env
nano ~/learnify-platform/.env
```

**Обновите URL:**

```env
NEXT_PUBLIC_APP_URL="https://ваш-домен.ru"
```

**Сохраните:** Ctrl+X → Y → Enter

```bash
# Перезапустите приложение
pm2 restart learnify
```

### 11.5 Проверка

Откройте в браузере:
```
https://ваш-домен.ru
```

**Вы должны увидеть:**
- ✅ Зеленый замочек (SSL работает)
- ✅ Главная страница Learnify
- ✅ Все работает без ошибок

---

## 12. Финальное тестирование

### 12.1 Чек-лист функциональности

Пройдитесь по всем страницам и проверьте:

#### Публичные страницы

- [ ] **Главная страница** (`/`)
  - Загружается
  - Hero секция видна
  - Featured курсы отображаются
  - Нет ошибок в консоли (F12)

- [ ] **Каталог курсов** (`/courses`)
  - Список курсов загружается
  - Фильтры работают
  - Поиск работает
  - Карточки кликабельны

- [ ] **Страница курса** (`/courses/[slug]`)
  - Детали курса видны
  - Список уроков отображается
  - Цена показывается
  - Кнопка "Enroll" работает

- [ ] **О нас** (`/about`)
  - Страница загружается

- [ ] **FAQ** (`/faq`)
  - Страница загружается

#### Авторизация

- [ ] **Страница логина** (`/login`)
  - Форма логина отображается
  - Memberstack виджет загружается
  - Можно зарегистрироваться
  - Можно войти

**Создайте тестовый аккаунт:**
```
1. Откройте /login
2. Нажмите "Sign Up"
3. Введите email и пароль
4. Подтвердите email (проверьте почту)
5. Войдите в аккаунт
```

#### Защищенные страницы (после логина)

- [ ] **Dashboard** (`/dashboard`)
  - Показывает ваши курсы
  - Прогресс отображается
  - Статистика видна

- [ ] **Wishlist** (`/wishlist`)
  - Можно добавить курс в избранное
  - Список избранного отображается

- [ ] **Enrollment (запись на курс)**
  - Для бесплатного курса: сразу записывает
  - Для платного: показывает Memberstack payment

- [ ] **Lesson player** (`/learn/[course]/[lesson]`)
  - Урок открывается
  - Видео (если есть) загружается
  - Можно отметить как завершенный
  - Навигация между уроками работает

#### Админ панель (нужны admin права)

**Чтобы получить admin доступ:**

1. **Узнайте ваш Memberstack User ID:**
   ```
   В браузере:
   1. Откройте консоль (F12)
   2. Введите: localStorage.getItem('_ms-mid')
   3. Скопируйте ID
   ```

2. **Добавьте в .env на сервере:**
   ```bash
   # На сервере
   nano ~/learnify-platform/.env
   
   # Добавьте:
   NEXT_PUBLIC_ADMIN_USER_IDS="ваш_memberstack_user_id"
   
   # Сохраните и перезапустите
   pm2 restart learnify
   ```

3. **Проверьте admin панель:**
   ```
   https://ваш-домен.ru/admin
   ```

- [ ] **Admin Dashboard** (`/admin`)
  - Статистика отображается
  - Графики работают

- [ ] **Управление курсами** (`/admin/courses`)
  - Список курсов
  - Можно создать новый курс
  - Можно редактировать
  - Можно удалить

- [ ] **Управление пользователями** (`/admin/users`)
  - Список пользователей (из Memberstack)

- [ ] **Analytics** (`/admin/analytics`)
  - Метрики отображаются

### 12.2 Проверка производительности

#### Lighthouse Audit

1. **Откройте сайт в Chrome**
2. **Откройте DevTools** (F12)
3. **Перейдите на вкладку Lighthouse**
4. **Нажмите "Analyze page load"**

**Целевые показатели:**
- Performance: > 80
- Accessibility: > 90
- Best Practices: > 90
- SEO: > 90

#### Проверка мобильной версии

1. **Откройте DevTools** (F12)
2. **Toggle device toolbar** (Ctrl+Shift+M)
3. **Выберите iPhone или Android**
4. **Проверьте:**
   - Навигация работает
   - Текст читабелен
   - Кнопки кликабельны
   - Все адаптивно

### 12.3 Проверка логов

```bash
# На сервере
# Проверьте логи приложения
pm2 logs learnify --lines 50

# Проверьте логи Nginx
sudo tail -f /var/log/nginx/learnify-error.log
```

**Не должно быть:**
- Ошибок подключения к БД
- 500 ошибок
- Критических ошибок

### 12.4 Проверка email

**Отправьте тестовый email:**

Если у вас есть функционал отправки email (например welcome email при регистрации):

1. Зарегистрируйте нового пользователя
2. Проверьте что email пришел
3. Проверьте что ссылки в email работают

---

## 13. Мониторинг и обслуживание

### 13.1 Настройка мониторинга

#### PM2 мониторинг

```bash
# Установите PM2 monitoring (опционально)
pm2 install pm2-logrotate

# Настройте ротацию логов
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7
```

#### UptimeRobot (бесплатный мониторинг)

1. **Перейдите на** https://uptimerobot.com
2. **Зарегистрируйтесь**
3. **Создайте monitor:**
   - Type: HTTP(s)
   - URL: https://ваш-домен.ru
   - Interval: 5 minutes
   - Alert contacts: ваш email
4. **Сохраните**

Теперь вы будете получать уведомления если сайт упал.

#### Логирование

**Настройте ротацию логов Nginx:**

```bash
# Создайте конфиг logrotate
sudo nano /etc/logrotate.d/learnify
```

```
/var/log/nginx/learnify-*.log {
    daily
    rotate 7
    compress
    delaycompress
    notifempty
    create 0640 www-data adm
    sharedscripts
    postrotate
        [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`
    endscript
}
```

### 13.2 Регулярные задачи

#### Еженедельно

- [ ] Проверьте логи на ошибки
  ```bash
  pm2 logs learnify --err --lines 100
  ```

- [ ] Проверьте использование ресурсов
  ```bash
  pm2 monit
  ```

- [ ] Проверьте место на диске
  ```bash
  df -h
  ```

- [ ] Проверьте бэкапы БД в Timeweb панели

#### Ежемесячно

- [ ] Обновите зависимости (на dev машине, потом deploy)
  ```bash
  npm outdated
  npm update
  npm audit fix
  ```

- [ ] Обновите систему
  ```bash
  sudo apt update && sudo apt upgrade -y
  ```

- [ ] Проверьте SSL сертификат
  ```bash
  sudo certbot certificates
  ```

### 13.3 Бэкапы

#### Автоматические бэкапы БД (Timeweb)

1. **В панели Timeweb:**
   ```
   Базы данных → Ваша БД → Настройки → Резервные копии
   ```

2. **Включите автобэкапы:**
   - Частота: ежедневно
   - Хранить: 7 дней
   - Время: 3:00 ночи

#### Ручной бэкап

```bash
# На сервере
# Создайте бэкап
pg_dump -h ваш-хост.timeweb.cloud -U postgres -d learnify > backup_$(date +%Y%m%d).sql

# Скачайте на локальный компьютер
# На вашем компьютере:
scp learnify@ВАШ_IP:~/backup_*.sql ./backups/
```

#### Бэкап кода

Git уже является бэкапом, но можно дополнительно:

```bash
# На сервере
cd ~/learnify-platform
tar -czf ~/backups/learnify_$(date +%Y%m%d).tar.gz .
```

### 13.4 Обновление приложения

**Когда вы внесли изменения в код:**

```bash
# 1. На локальном компьютере
git add .
git commit -m "Update: описание изменений"
git push origin main

# 2. На сервере
cd ~/learnify-platform
git pull origin main

# 3. Установите новые зависимости (если добавились)
npm install

# 4. Примените миграции БД (если есть)
npx prisma migrate deploy

# 5. Пересоберите приложение
npm run build

# 6. Перезапустите
pm2 restart learnify

# 7. Проверьте логи
pm2 logs learnify --lines 50
```

---

## 14. Решение проблем

### 14.1 Приложение не запускается

**Симптомы:**
```
pm2 status
# learnify | errored
```

**Решение:**

```bash
# Проверьте логи
pm2 logs learnify --err

# Частые причины:
```

**1. Ошибка подключения к БД**
```
Error: Can't reach database server
```

Решение:
```bash
# Проверьте DATABASE_URL в .env
nano ~/.env

# Проверьте что БД доступна
pg_isready -h ваш-хост.timeweb.cloud -p 5432

# Проверьте firewall на БД в Timeweb панели
```

**2. Порт 3000 занят**
```
Error: Port 3000 is already in use
```

Решение:
```bash
# Найдите процесс
lsof -i :3000

# Убейте процесс
kill -9 PID

# Или смените порт в package.json:
# "start": "next start -p 3001"
```

**3. Недостаточно памяти**
```
JavaScript heap out of memory
```

Решение:
```bash
# Увеличьте heap size
pm2 delete learnify
pm2 start npm --name "learnify" --node-args="--max-old-space-size=2048" -- start
```

### 14.2 Страницы показывают 500 ошибку

**Симптомы:**
```
Internal Server Error
```

**Решение:**

```bash
# Проверьте логи
pm2 logs learnify --lines 100

# Проверьте Nginx логи
sudo tail -n 100 /var/log/nginx/learnify-error.log
```

**Частые причины:**

1. **Ошибка в API route**
   - Проверьте логи приложения
   - Исправьте код
   - Пересоберите и перезапустите

2. **Проблема с БД**
   - Проверьте подключение
   - Проверьте что миграции применены

3. **Отсутствует env переменная**
   - Проверьте .env файл
   - Убедитесь что все переменные заполнены

### 14.3 Memberstack не работает

**Симптомы:**
```
Cannot read property 'user' of undefined
```

**Решение:**

1. **Проверьте ключи:**
   ```bash
   # В .env должны быть:
   NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY="pk_..."
   MEMBERSTACK_SECRET_KEY="sk_..."
   ```

2. **Проверьте домен в Memberstack:**
   - Зайдите в Memberstack панель
   - Settings → Allowed Domains
   - Добавьте ваш домен

3. **Проверьте браузер:**
   - Откройте DevTools (F12)
   - Console → должен загрузиться @memberstack/dom
   - Network → проверьте запросы к Memberstack

### 14.4 SSL сертификат не работает

**Симптомы:**
```
NET::ERR_CERT_AUTHORITY_INVALID
```

**Решение:**

```bash
# Проверьте сертификат
sudo certbot certificates

# Обновите сертификат
sudo certbot renew

# Проверьте Nginx конфиг
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### 14.5 Медленная загрузка

**Решение:**

1. **Включите компрессию в Nginx:**
   ```bash
   sudo nano /etc/nginx/nginx.conf
   ```
   
   Добавьте в `http {}` блок:
   ```nginx
   gzip on;
   gzip_vary on;
   gzip_proxied any;
   gzip_comp_level 6;
   gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;
   ```
   
   ```bash
   sudo nginx -t
   sudo systemctl restart nginx
   ```

2. **Оптимизируйте изображения:**
   - Используйте WebP формат
   - Сжимайте изображения
   - Используйте CDN (Cloudinary уже настроен)

3. **Включите кэширование:**
   - Уже настроено в Nginx конфиге выше
   - Проверьте headers в DevTools

### 14.6 База данных заполнена

**Симптомы:**
```
Error: disk full
```

**Решение:**

1. **Проверьте место:**
   ```bash
   df -h
   ```

2. **В Timeweb панели:**
   - Увеличьте размер диска БД
   - Или почистите старые данные

3. **Очистка:**
   ```sql
   -- Удалите старые логи/данные
   DELETE FROM comments WHERE createdAt < NOW() - INTERVAL '6 months';
   ```

### 14.7 Получение помощи

**Если проблема не решается:**

1. **Проверьте документацию:**
   - Next.js: https://nextjs.org/docs
   - Prisma: https://www.prisma.io/docs
   - Memberstack: https://docs.memberstack.com

2. **Проверьте существующие issues:**
   - GitHub репозиторий
   - Stack Overflow
   - Форумы Timeweb

3. **Создайте issue:**
   - Опишите проблему
   - Приложите логи
   - Укажите версии

4. **Обратитесь в поддержку:**
   - Timeweb: support@timeweb.ru
   - Telegram: @timeweb_cloud

---

## 🎉 Поздравляем!

Ваша платформа Learnify успешно развернута на production!

### Что теперь?

1. **Создайте контент:**
   - Добавьте реальные курсы
   - Загрузите видео на Vimeo
   - Настройте ценыв Memberstack

2. **Настройте маркетинг:**
   - SEO оптимизация
   - Google Analytics
   - Социальные сети

3. **Мониторьте:**
   - Проверяйте логи
   - Следите за производительностью
   - Отвечайте на feedback

4. **Развивайте:**
   - Добавляйте новые функции
   - Улучшайте UX
   - Слушайте пользователей

---

## 📞 Поддержка

**Если нужна помощь:**

- 📧 Email: support@ваш-домен.ru (создайте)
- 💬 Telegram: @ваш_канал
- 📖 Документация: /docs
- 🐛 Issues: GitHub

---

## 📚 Дополнительные ресурсы

### Официальная документация

- **Next.js:** https://nextjs.org/docs
- **Prisma:** https://www.prisma.io/docs
- **Memberstack:** https://docs.memberstack.com
- **Timeweb Cloud:** https://timeweb.cloud/help

### Полезные статьи

- Оптимизация Next.js для production
- Безопасность Node.js приложений
- PostgreSQL best practices
- Nginx configuration guide

### Инструменты мониторинга

- **UptimeRobot:** https://uptimerobot.com
- **Google Analytics:** https://analytics.google.com
- **Sentry:** https://sentry.io (для error tracking)
- **Plausible:** https://plausible.io (privacy-friendly analytics)

---

**Версия руководства:** 1.0.0  
**Дата:** 12 февраля 2026  
**Статус:** ✅ Production Ready

**Удачи с вашей платформой! 🚀🎓✨**

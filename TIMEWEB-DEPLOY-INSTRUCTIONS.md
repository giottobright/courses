# 🚀 Инструкция по деплою на TimeWeb Cloud - ПОШАГОВО

## ✅ Что уже сделано:

1. ✅ Tailwind CSS перенесен в `dependencies`
2. ✅ Создан правильный multi-stage `Dockerfile`
3. ✅ Добавлен `.dockerignore` для оптимизации
4. ✅ Включен `output: 'standalone'` в `next.config.mjs`
5. ✅ Все изменения запушены на GitHub (коммит `2a0ee01`)

---

## 📋 ЧТО ВАМ НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС:

### Шаг 1: Откройте панель TimeWeb Cloud

1. Откройте браузер
2. Перейдите на https://timeweb.cloud
3. Войдите в свой аккаунт
4. Перейдите в раздел **"Приложения"** или **"App Platform"**

### Шаг 2: Найдите ваше приложение

1. В списке приложений найдите `giottobright-courses`
2. Кликните на него, чтобы открыть детали

### Шаг 3: Запустите пересборку

**ВАЖНО:** Есть два способа:

#### Вариант А: Автоматический деплой (если настроен)
- Если у вас настроен автоматический деплой из GitHub
- Просто подождите 2-3 минуты - TimeWeb сам подтянет изменения

#### Вариант Б: Ручной деплой (рекомендуется)
1. Найдите кнопку **"Rebuild"** / **"Пересобрать"** / **"Deploy"**
2. Нажмите на неё
3. Подтвердите действие

### Шаг 4: Дождитесь завершения сборки

1. Откройте вкладку **"Логи"** или **"Build Logs"**
2. Следите за процессом сборки (3-5 минут)
3. Дождитесь сообщения:
   ```
   ✓ Compiled successfully
   ✓ Generating static pages
   Build succeeded
   Deploy succeeded
   ```

### Шаг 5: Очистите кэш браузера

**КРИТИЧЕСКИ ВАЖНО!** Старая версия может быть в кэше:

1. Откройте ваш сайт
2. Нажмите **Ctrl + Shift + R** (Windows) или **Cmd + Shift + R** (Mac)
3. Или: 
   - Нажмите F12 → откройте DevTools
   - Правый клик на кнопке обновления → "Empty Cache and Hard Reload"

### Шаг 6: Проверьте что всё работает

#### 6.1 Визуальная проверка:
- ✅ Сайт должен иметь **полный дизайн** с цветами
- ✅ Кнопки должны быть стилизованы (фиолетовые, оранжевые)
- ✅ Карточки курсов должны иметь закругленные углы
- ✅ Шрифты должны быть правильными

#### 6.2 Проверка консоли:
1. Нажмите F12 → откройте вкладку **Console**
2. **НЕ должно быть** ошибок:
   - ❌ `SyntaxError: Unexpected token '<'`
   - ❌ `Failed to load resource: 404`
   - ❌ `net::ERR_ABORTED 404`

#### 6.3 Проверка Network:
1. Нажмите F12 → откройте вкладку **Network**
2. Обновите страницу (F5)
3. Найдите файлы вида `_next/static/css/...`
4. Все CSS/JS файлы должны загружаться со статусом **200** (зеленый)

---

## 🎯 Тестирование всех страниц:

После успешного деплоя протестируйте каждую страницу:

### Список страниц для проверки:

1. **Главная страница** (`/`)
   - Должен быть hero-баннер с градиентом
   - Карточки категорий (Marketing, Psychology и т.д.)
   - Кнопки должны быть стилизованы

2. **О нас** (`/about`)
   - Hero-секция с градиентом
   - Карточки ценностей с иконками
   - Секция "Our Story" с цветным фоном

3. **Курсы** (`/courses`)
   - Список курсов с карточками
   - Фильтры должны работать
   - Изображения курсов загружаются

4. **FAQ** (`/faq`)
   - Аккордеон с вопросами
   - Анимация открытия/закрытия

5. **Логин** (`/login`)
   - Форма входа стилизована
   - Кнопки работают

6. **Dashboard** (`/dashboard`)
   - Требует авторизации
   - После входа должен показывать личный кабинет

---

## ❌ Если проблема НЕ решилась:

### Проверьте логи сборки:

1. Зайдите в TimeWeb Cloud → ваше приложение
2. Откройте вкладку "Логи"
3. Найдите последнюю сборку
4. Ищите строку с последним коммитом: `2a0ee01`
5. Если коммит старый - значит TimeWeb не подтянул изменения

### Принудительная пересборка:

Если автоматический деплой не сработал:

1. Зайдите в настройки приложения
2. Найдите раздел "GitHub" или "Source"
3. Нажмите "Reconnect" или "Sync"
4. Затем снова "Rebuild"

### Проверьте webhook GitHub:

1. Откройте https://github.com/giottobright/courses/settings/hooks
2. Убедитесь что есть webhook для TimeWeb Cloud
3. Проверьте что последняя доставка была успешной (зеленая галочка)

---

## 📸 Что должно получиться:

### ДО исправления:
```
❌ Белая страница без стилей
❌ Только текст и ссылки
❌ Консоль полна ошибок SyntaxError
❌ CSS файлы возвращают 404
```

### ПОСЛЕ исправления:
```
✅ Полностью стилизованный сайт
✅ Все цвета, градиенты, анимации работают
✅ Консоль чистая (без ошибок)
✅ Все статические файлы загружаются (200 OK)
✅ Tailwind CSS применяется
✅ Framer Motion анимации работают
```

---

## 🔧 Дополнительная отладка:

### Если CSS всё ещё не загружается:

1. Проверьте URL статических файлов в DevTools → Network:
   - Правильно: `https://giottobright-courses-3499.twc1.net/_next/static/css/...`
   - Неправильно: `https://giottobright-courses-3499.twc1.net/css/...`

2. Откройте файл напрямую:
   - Скопируйте URL из Network tab
   - Откройте в новой вкладке
   - Должен загрузиться CSS код, а не HTML страница 404

3. Проверьте заголовки ответа:
   - `Content-Type: text/css` ✅
   - `Content-Type: text/html` ❌ (означает что файл не найден)

---

## 📞 Если ничего не помогло:

1. Сделайте скриншот:
   - Консоли (F12 → Console)
   - Network tab (F12 → Network)
   - Логов сборки из TimeWeb Cloud

2. Пришлите скриншоты

3. Укажите:
   - URL вашего сайта
   - Что именно не работает
   - Сообщения об ошибках из консоли

---

## ✅ Финальный чеклист:

- [ ] Открыл TimeWeb Cloud
- [ ] Нашел своё приложение
- [ ] Запустил Rebuild/Пересборку
- [ ] Дождался "Build succeeded"
- [ ] Очистил кэш браузера (Ctrl+Shift+R)
- [ ] Открыл сайт
- [ ] Проверил что дизайн есть
- [ ] Проверил консоль (F12) - нет ошибок
- [ ] Протестировал все страницы
- [ ] Всё работает! 🎉

---

**СЕЙЧАС ИДИТЕ И СДЕЛАЙТЕ ЭТО!** ⬆️

После выполнения всех шагов - сообщите результат или пришлите скриншот.

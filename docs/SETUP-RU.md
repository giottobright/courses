# 🚀 Руководство по запуску Learnify

Полное руководство по установке и запуску платформы онлайн-обучения на локальном компьютере.

## 📋 Что нужно перед началом

- **Node.js** версии 18 или выше ([Скачать](https://nodejs.org/))
- **npm** (устанавливается вместе с Node.js)
- Редактор кода (рекомендуется [VS Code](https://code.visualstudio.com/))
- Терминал (PowerShell, CMD или встроенный терминал VS Code)

## 🎯 Быстрый старт (5 минут)

### Шаг 1: Откройте терминал в папке проекта

В Windows PowerShell перейдите в папку:

```powershell
cd C:\Users\User\Desktop\courses
```

Или откройте папку в VS Code и используйте встроенный терминал (`` Ctrl + ` ``).

### Шаг 2: Установите зависимости

Выполните команду:

```bash
npm install
```

Это установит все необходимые библиотеки. Процесс займет 2-3 минуты.

> **Что происходит?** npm скачивает все пакеты, указанные в `package.json`: Next.js, React, Tailwind CSS, Framer Motion и другие.

### Шаг 3: Создайте файл окружения (опционально)

Если планируете подключать Memberstack для аутентификации:

```bash
# Windows PowerShell
copy .env.local.example .env.local
```

Затем откройте `.env.local` и добавьте свой ID от Memberstack.

> **Примечание**: Для тестирования дизайна этот шаг можно пропустить. Сайт будет работать с демо-данными.

### Шаг 4: Запустите сервер разработки

```bash
npm run dev
```

### Шаг 5: Откройте браузер

Перейдите по адресу: **http://localhost:3000**

🎉 **Готово!** Сайт запущен и готов к работе.

## 📱 Как тестировать на разных устройствах

### Десктоп
Просто откройте http://localhost:3000 в браузере.

### Мобильные устройства (в локальной сети)

1. Узнайте IP-адрес вашего компьютера:

```powershell
ipconfig
```

Найдите "IPv4 Address" (например, 192.168.1.100)

2. На телефоне/планшете откройте браузер и перейдите:

```
http://192.168.1.100:3000
```

> **Важно**: Телефон и компьютер должны быть в одной Wi-Fi сети.

### Тестирование адаптивности в браузере

1. Откройте DevTools (F12)
2. Нажмите на иконку устройств (Ctrl + Shift + M)
3. Выберите нужное устройство из списка

## 🎨 Обзор страниц

После запуска доступны следующие страницы:

| Страница | URL | Описание |
|----------|-----|----------|
| Главная | `/` | Лендинг с героем и популярными курсами |
| Каталог курсов | `/courses` | Все курсы с фильтрами |
| Детали курса | `/courses/creative-writing-beginners` | Подробная страница курса |
| О нас | `/about` | Информация о платформе |
| FAQ | `/faq` | Часто задаваемые вопросы |
| Вход | `/login` | Страница аутентификации |
| Дашборд | `/dashboard` | Личный кабинет пользователя |
| Урок | `/learn/creative-writing-beginners/intro-creative-writing` | Просмотр урока |
| Сертификат | `/certificates/1` | Страница сертификата |

## 🎨 Цветовая схема

Проект использует яркую, современную палитру:

- **Фиолетовый** (`#b4a0d8`, `#8b5cf6`) - основной цвет
- **Оранжевый** (`#ff5722`) - акцент, CTA кнопки
- **Желтый** (`#ffc107`) - акцент, карточки
- **Розовый** (`#ff6b9d`) - акцент
- **Темный** (`#2d2d2d`) - текст
- **Кремовый** (`#faf8f5`) - фон

## 🛠 Команды разработки

```bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшн-версии (после build)
npm start

# Проверка кода линтером
npm run lint
```

## 📁 Структура проекта

```
courses/
├── src/
│   ├── app/                 # Страницы Next.js
│   │   ├── page.tsx        # Главная страница
│   │   ├── courses/        # Каталог и страницы курсов
│   │   ├── dashboard/      # Дашборд пользователя
│   │   ├── learn/          # Плеер уроков
│   │   └── ...
│   │
│   ├── components/         # React компоненты
│   │   ├── ui/            # UI компоненты (кнопки, карточки)
│   │   └── layout/        # Navbar, Footer
│   │
│   ├── data/              # Данные
│   │   └── courses.ts     # Курсы, категории, статистика
│   │
│   └── types/             # TypeScript типы
│
├── public/                # Статические файлы
├── README.md             # Документация (English)
├── SETUP-RU.md          # Это руководство
└── package.json         # Зависимости проекта
```

## 🎯 Редактирование контента

### Добавление нового курса

Откройте `src/data/courses.ts` и добавьте новый объект в массив `courses`:

```typescript
{
  id: '7',
  title: 'Название вашего курса',
  slug: 'nazvanie-vashego-kursa',
  description: 'Полное описание...',
  shortDescription: 'Краткое описание',
  category: 'Marketing',
  price: 49,
  // ... остальные поля
}
```

### Изменение цветов

Откройте `tailwind.config.ts` и измените цвета в секции `theme.extend.colors`.

### Изменение шрифтов

Шрифты настраиваются в `src/app/layout.tsx`:
- **Inter** - для основного текста
- **Space Grotesk** - для заголовков

## 🔌 Подключение Memberstack

Memberstack - это сервис для управления пользователями и подписками.

### Настройка:

1. Зарегистрируйтесь на [memberstack.com](https://memberstack.com)
2. Создайте новое приложение
3. Получите App ID
4. Создайте файл `.env.local`:

```env
NEXT_PUBLIC_MEMBERSTACK_ID=ваш_app_id
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

5. Перезапустите сервер (`npm run dev`)

Страница `/login` уже готова - нужно только подключить обработчики Memberstack.

## 🚀 Деплой (публикация в интернет)

### Vercel (рекомендуется, бесплатно)

1. Загрузите код на GitHub
2. Зайдите на [vercel.com](https://vercel.com)
3. Нажмите "Import Project"
4. Выберите ваш репозиторий
5. Добавьте переменные окружения (если нужны)
6. Нажмите "Deploy"

Готово! Ваш сайт будет доступен по адресу типа `your-project.vercel.app`.

### Другие платформы

Можно также использовать:
- **Netlify** - аналог Vercel
- **Railway** - с поддержкой баз данных
- **AWS Amplify** - от Amazon
- **Digital Ocean** - классический хостинг

## 💡 Полезные советы

### Горячая перезагрузка
При сохранении файлов страница автоматически обновляется. Не нужно перезапускать сервер.

### Остановка сервера
Нажмите `Ctrl + C` в терминале.

### Очистка кеша
Если что-то работает странно:

```bash
# Удалите папки
rm -rf node_modules .next

# Переустановите
npm install
npm run dev
```

### Ошибки портов
Если порт 3000 занят, Next.js предложит использовать другой (например, 3001).

## 🐛 Решение проблем

### "npm не является внутренней или внешней командой"

**Проблема**: Node.js не установлен или не добавлен в PATH.

**Решение**: 
1. Установите Node.js с [nodejs.org](https://nodejs.org/)
2. Перезапустите терминал
3. Проверьте: `node --version`

### Ошибка "Cannot find module"

**Проблема**: Не установлены зависимости.

**Решение**: Выполните `npm install`

### Порт уже используется

**Проблема**: Порт 3000 занят другим приложением.

**Решение**: 
- Закройте другое приложение
- Или используйте другой порт: `npm run dev -- -p 3001`

### Медленная установка пакетов

**Решение**: Используйте зеркало npm:

```bash
npm config set registry https://registry.npmjs.org/
```

## 📚 Дополнительные ресурсы

- [Next.js документация](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Framer Motion](https://www.framer.com/motion/)
- [TypeScript](https://www.typescriptlang.org/docs/)

## 🎨 Кастомизация дизайна

### Изменить основной цвет

В `tailwind.config.ts`:

```typescript
colors: {
  primary: {
    // Измените эти значения
    500: '#8b5cf6', // Ваш цвет
  }
}
```

### Добавить новые компоненты

Создайте файл в `src/components/ui/YourComponent.tsx`:

```typescript
export const YourComponent = () => {
  return <div>Ваш компонент</div>;
};
```

### Изменить шаблон страницы

Откройте `src/app/page.tsx` и редактируйте JSX.

## 🎬 Анимации

Проект использует Framer Motion для плавных анимаций:

```typescript
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5 }}
>
  Ваш контент
</motion.div>
```

## ✅ Чек-лист для тестирования

- [ ] Главная страница загружается
- [ ] Навигация работает
- [ ] Каталог курсов отображается
- [ ] Фильтры курсов работают
- [ ] Страница курса открывается
- [ ] Дашборд отображается
- [ ] Плеер урока работает
- [ ] Сертификат генерируется
- [ ] Адаптивный дизайн на мобильных
- [ ] Все анимации плавные

## 📞 Поддержка

Если возникли вопросы:

1. Проверьте это руководство
2. Посмотрите [README.md](./README.md) (English)
3. Проверьте консоль браузера на ошибки (F12)
4. Проверьте терминал на ошибки сервера

---

**Удачи с разработкой! 🚀✨**

*Создано с любовью к современному веб-дизайну*

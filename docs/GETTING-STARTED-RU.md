# 🚀 Начало работы с Learnify

Пошаговое руководство по запуску платформы с нуля.

---

## 📋 Что вам понадобится

### Обязательно:
- ✅ Node.js 18+ ([скачать](https://nodejs.org/))
- ✅ PostgreSQL 14+ ([скачать](https://www.postgresql.org/download/) или Docker)
- ✅ Git ([скачать](https://git-scm.com/))
- ✅ Текстовый редактор (VS Code рекомендуется)

### Аккаунты в сервисах (бесплатные планы доступны):
- ✅ [Memberstack](https://www.memberstack.com) - аутентификация + платежи
- ✅ [Vimeo Pro](https://vimeo.com/upgrade) - видео хостинг ($20/мес)
- ✅ [Cloudinary](https://cloudinary.com) - изображения (free tier)
- ✅ [SendGrid](https://sendgrid.com) - emails (free tier)

---

## 🎯 Шаг 1: Клонирование и установка

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd courses

# Установите зависимости
npm install
```

**Время:** ~3 минуты (зависит от скорости интернета)

---

## 🗄️ Шаг 2: Настройка базы данных

### Вариант A: Локально (если у вас установлен PostgreSQL)

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE learnify;

# Создайте пользователя (опционально)
CREATE USER learnify_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE learnify TO learnify_user;

# Выйдите
\q
```

**Connection string:**
```
postgresql://postgres:password@localhost:5432/learnify
```

### Вариант B: Docker (если нет PostgreSQL)

```bash
# Запустите PostgreSQL в контейнере
docker run --name learnify-postgres \
  -e POSTGRES_DB=learnify \
  -e POSTGRES_USER=learnify \
  -e POSTGRES_PASSWORD=password \
  -p 5432:5432 \
  -d postgres:14

# Проверьте что контейнер запущен
docker ps
```

**Connection string:**
```
postgresql://learnify:password@localhost:5432/learnify
```

### Вариант C: Облачный сервис (самый простой)

1. Зарегистрируйтесь на [Supabase](https://supabase.com) (бесплатно)
2. Создайте новый проект
3. Скопируйте Connection String из Settings → Database
4. Используйте этот URL в `.env`

**Время:** ~5 минут

---

## ⚙️ Шаг 3: Настройка Environment Variables

### 1. Создайте файл `.env`

```bash
# Скопируйте шаблон
cp ENV-TEMPLATE.md .env
```

### 2. Заполните обязательные переменные

Откройте `.env` в редакторе и заполните:

```bash
# ========================================
# ОБЯЗАТЕЛЬНО ДЛЯ ЗАПУСКА
# ========================================

# Database (из Шага 2)
DATABASE_URL="postgresql://user:password@localhost:5432/learnify"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-min-32-characters-long"

# Memberstack (создадим в следующем шаге)
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY="pk_sb_..."
MEMBERSTACK_SECRET_KEY="sk_sb_..."
MEMBERSTACK_WEBHOOK_SECRET="whsec_..."
```

### 3. Генерируйте секретный ключ

```bash
# В терминале:
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Скопируйте результат в NEXTAUTH_SECRET
```

**Остальные переменные** (Vimeo, Cloudinary, SendGrid) можно настроить позже.

**Время:** ~5 минут

---

## 🔐 Шаг 4: Настройка Memberstack

### 1. Создайте аккаунт

1. Перейдите на [Memberstack.com](https://www.memberstack.com)
2. Нажмите "Get Started Free"
3. Заполните форму регистрации
4. Подтвердите email

### 2. Создайте App

1. В Dashboard нажмите "Create New App"
2. Назовите: "Learnify"
3. Выберите тип: "Web App"

### 3. Получите API Keys

1. Перейдите в **Settings → API Keys**
2. Скопируйте **Public Key** → в `.env` как `NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY`
3. Скопируйте **Secret Key** → в `.env` как `MEMBERSTACK_SECRET_KEY`

### 4. Подключите Stripe

1. Перейдите в **Settings → Payments**
2. Нажмите **"Connect with Stripe"**
3. Войдите в Stripe или создайте аккаунт
4. Подтвердите подключение

✅ **Готово!** Stripe теперь интегрирован через Memberstack.

### 5. Создайте Test Plan (для тестирования)

1. Перейдите в **Plans → Create Plan**
2. Заполните:
   - Name: "Test Course Plan"
   - Price: $9.99
   - Type: One-time payment
3. Сохраните
4. **Скопируйте Plan ID** (например: `pln_abc123`)

**Время:** ~10 минут

**Подробнее:** См. `MEMBERSTACK-SETUP.md`

---

## 🏗️ Шаг 5: Инициализация базы данных

```bash
# 1. Сгенерируйте Prisma Client
npm run prisma:generate

# 2. Примените миграции (создаст таблицы)
npm run prisma:migrate

# 3. Заполните тестовыми данными (опционально)
npm run prisma:seed
```

**Что создаётся:**
- 10 таблиц (categories, courses, lessons, и т.д.)
- 6 категорий курсов
- 3 примера курсов
- 1 курс с уроками для тестирования

**Время:** ~2 минуты

---

## 🎮 Шаг 6: Запуск проекта!

```bash
# Запустите dev сервер
npm run dev
```

**Откройте в браузере:** http://localhost:3000

### ✅ Что должно работать:

1. **Homepage** - главная страница отображается
2. **Courses** - каталог курсов (если загружен seed)
3. **Registration** - можно зарегистрироваться через Memberstack
4. **Free Enrollment** - можно записаться на бесплатный курс

**Время:** ~1 минута

---

## 🧪 Шаг 7: Первый тест

### 1. Зарегистрируйтесь как пользователь

```
1. Откройте http://localhost:3000
2. Нажмите "Sign Up" (или перейдите на /signup)
3. Введите email и password
4. Подтвердите регистрацию
```

### 2. Запишитесь на бесплатный курс

```
1. Перейдите в Courses (/courses)
2. Выберите бесплатный курс (price = 0)
3. Нажмите "Enroll Now"
4. Проверьте что появился доступ к урокам
```

### 3. Проверьте Dashboard

```
1. Перейдите в Dashboard (/dashboard)
2. Проверьте что курс отображается
3. Нажмите "Continue Learning"
4. Откроется lesson player
```

### 4. Тест платного курса (опционально)

```
1. Откройте платный курс
2. Нажмите "Enroll Now"
3. Должен открыться Stripe Checkout
4. Используйте тестовую карту: 4242 4242 4242 4242
5. Завершите покупку
6. Проверьте что создался enrollment
```

**Время:** ~10 минут

---

## 🎨 Шаг 8: Добавьте свой контент

### Создайте первый курс через Admin Panel

1. **Получите admin доступ:**
   ```bash
   # В .env добавьте ваш Memberstack User ID
   NEXT_PUBLIC_ADMIN_USER_IDS="your_memberstack_user_id"
   ```

2. **Откройте Admin Panel:**
   ```
   http://localhost:3000/admin
   ```

3. **Создайте курс:**
   - Нажмите "Create Course"
   - Заполните информацию
   - Для платного курса добавьте `memberstackPlanId` из Шага 4
   - Сохраните

4. **Загрузите видео на Vimeo:**
   - Зарегистрируйтесь на Vimeo Pro
   - Загрузите видео урока
   - Скопируйте Vimeo ID
   - Добавьте в Lesson record

**Время:** ~20 минут

---

## 📚 Шаг 9: Настройте остальные интеграции

### Vimeo (для видео уроков)

1. Подпишитесь на Vimeo Pro: https://vimeo.com/upgrade
2. Получите Access Token: https://developer.vimeo.com/apps
3. Добавьте в `.env`:
   ```bash
   VIMEO_ACCESS_TOKEN="your_token"
   ```

### Cloudinary (для изображений)

1. Зарегистрируйтесь: https://cloudinary.com
2. Получите credentials из Dashboard
3. Добавьте в `.env`:
   ```bash
   CLOUDINARY_CLOUD_NAME="your_cloud"
   CLOUDINARY_API_KEY="your_key"
   CLOUDINARY_API_SECRET="your_secret"
   ```

### SendGrid (для emails)

1. Зарегистрируйтесь: https://sendgrid.com
2. Создайте API Key
3. Verify sender email
4. Добавьте в `.env`:
   ```bash
   SENDGRID_API_KEY="SG.xxx"
   SENDGRID_FROM_EMAIL="noreply@yourdomain.com"
   ```

**Подробнее:** См. `INTEGRATION-GUIDE.md`

**Время:** ~30 минут (если делать всё сразу)

---

## 🚀 Шаг 10: Deploy на Production

### Вариант A: Vercel (самый простой)

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Login
vercel login

# 3. Deploy
vercel

# 4. Настройте environment variables в Vercel Dashboard
# 5. Обновите DATABASE_URL на production DB
# 6. Production deploy
vercel --prod
```

### Вариант B: Другие платформы

- **Netlify** - аналогично Vercel
- **Railway** - включает PostgreSQL
- **Timeweb Cloud** - российский хостинг

**Подробнее:** См. `DEPLOYMENT.md`

**Время:** ~1 час (с настройкой production DB и интеграций)

---

## ✅ Checklist готовности

### Минимум для локального запуска:
- [x] Node.js установлен
- [x] PostgreSQL запущен
- [x] Зависимости установлены (`npm install`)
- [x] `.env` файл создан
- [x] DATABASE_URL настроен
- [x] Memberstack Public/Secret keys добавлены
- [x] Prisma migrations выполнены
- [x] Dev сервер запущен (`npm run dev`)

### Для полной функциональности:
- [ ] Memberstack webhook настроен
- [ ] Stripe подключен через Memberstack
- [ ] Plans созданы в Memberstack
- [ ] Vimeo интеграция настроена
- [ ] Cloudinary интеграция настроена
- [ ] SendGrid интеграция настроена

### Для production:
- [ ] Production database создан
- [ ] Environment variables на production
- [ ] Domain настроен
- [ ] SSL certificate установлен
- [ ] Webhooks URLs обновлены
- [ ] Email sender verified

---

## 🐛 Проблемы и решения

### Проблема: "Cannot connect to database"

**Решение:**
```bash
# Проверьте что PostgreSQL запущен
pg_isready

# Проверьте DATABASE_URL в .env
echo $DATABASE_URL

# Попробуйте подключиться напрямую
psql "postgresql://user:password@localhost:5432/learnify"
```

### Проблема: "Memberstack is not defined"

**Решение:**
```bash
# Проверьте что SDK установлен
npm list @memberstack/dom

# Если нет, установите
npm install @memberstack/dom

# Проверьте NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY в .env
```

### Проблема: Prisma migration failed

**Решение:**
```bash
# Сбросьте БД (ОСТОРОЖНО: удалит все данные)
npm run prisma:migrate reset

# Или создайте новую миграцию
npx prisma migrate dev --name init
```

### Проблема: Port 3000 already in use

**Решение:**
```bash
# Найдите процесс на порту 3000
lsof -i :3000  # Mac/Linux
netstat -ano | findstr :3000  # Windows

# Убейте процесс или используйте другой порт
PORT=3001 npm run dev
```

---

## 📖 Следующие шаги

### 1. Изучите документацию:
- **README.md** - общий обзор
- **TECH-STACK.md** - архитектура
- **MEMBERSTACK-SETUP.md** - детальная настройка Memberstack

### 2. Протестируйте платформу:
- Создайте тестовый курс
- Запишитесь на курс
- Пройдите урок
- Получите сертификат

### 3. Настройте интеграции:
- Загрузите видео на Vimeo
- Настройте email templates в SendGrid
- Добавьте изображения курсов через Cloudinary

### 4. Кастомизируйте:
- Измените цвета в `tailwind.config.ts`
- Обновите тексты на Homepage
- Добавьте свой логотип

### 5. Deploy:
- Создайте production database
- Настройте domain
- Deploy на Vercel/Netlify
- Протестируйте production

---

## 💡 Полезные команды

```bash
# Development
npm run dev              # Запустить dev сервер
npm run build            # Собрать для production
npm run start            # Запустить production сервер
npm run lint             # Проверить код

# Database
npm run prisma:studio    # Открыть Prisma Studio (GUI)
npm run prisma:migrate   # Применить миграции
npm run prisma:seed      # Заполнить данными
npm run prisma:generate  # Сгенерировать Prisma Client

# Utilities
npx tsc --noEmit        # Проверить TypeScript
npx prettier --write .  # Форматировать код
```

---

## 🎉 Готово!

Теперь у вас запущена полнофункциональная платформа Learnify!

**Что дальше?**
1. Добавьте свои курсы
2. Настройте интеграции
3. Кастомизируйте дизайн
4. Протестируйте всё
5. Deploy на production
6. Запускайте! 🚀

---

## 📞 Помощь

- **Документация:** См. все `.md` файлы в проекте
- **Memberstack:** https://docs.memberstack.com/
- **Next.js:** https://nextjs.org/docs
- **Prisma:** https://www.prisma.io/docs

---

**Удачи с запуском! 💪**

*Если что-то не получается, проверьте документацию или создайте issue.*

# 🔧 Быстрая инструкция по исправлению localStorage SSR

## ✅ Что было исправлено

### 1. `src/store/userStore.ts` 
- ✅ Добавлена безопасная обертка `safeStorage` для localStorage
- ✅ Настроен Zustand persist middleware для работы с SSR
- ✅ Теперь работает как на сервере, так и на клиенте

### 2. `src/lib/certificate.ts`
- ✅ Добавлены проверки `typeof window !== 'undefined'` в функцию `generateCertificatePDF`
- ✅ Добавлены проверки `typeof window !== 'undefined'` в функцию `shareCertificate`
- ✅ Защита от вызова браузерных API на сервере

### 3. Все остальные файлы
- ✅ Проверены и уже имели правильную структуру
- ✅ Используют `'use client'` директиву где необходимо
- ✅ Имеют проверки браузерных API

## 🧪 Как проверить

### Шаг 1: Проверка локально
```bash
# Остановите текущий dev сервер если запущен (Ctrl+C)
npm run build
npm start
```

Откройте браузер и проверьте:
- http://localhost:3000/admin
- http://localhost:3000/courses
- http://localhost:3000/courses/creative-writing (или любой slug курса)

### Шаг 2: Проверка в консоли
Откройте консоль разработчика (F12) и убедитесь, что нет ошибок:
- ❌ `ReferenceError: localStorage is not defined` - не должно быть
- ❌ `ReferenceError: window is not defined` - не должно быть
- ❌ `ReferenceError: document is not defined` - не должно быть

### Шаг 3: Проверка Docker сборки
```bash
# Создайте Docker образ
docker build -t courses-app .

# Запустите контейнер
docker run -p 3000:3000 courses-app
```

Проверьте те же страницы: http://localhost:3000

## 📋 Что дальше

1. **Деплой на TimeWeb:**
   Следуйте инструкциям в `TIMEWEB-DEPLOYMENT-GUIDE.md`

2. **Если возникли другие ошибки:**
   - Проверьте переменные окружения (`.env`)
   - Убедитесь, что база данных подключена
   - Проверьте логи: `docker logs <container-id>`

3. **Для добавления новых компонентов:**
   - Используйте `'use client'` для интерактивных компонентов
   - Оборачивайте браузерные API в проверки `typeof window !== 'undefined'`
   - Используйте `useEffect` для логики, требующей браузерного окружения

## 💡 Полезные команды

```bash
# Проверка типов TypeScript
npm run type-check

# Линтинг кода
npm run lint

# Форматирование кода
npm run format

# Production сборка
npm run build

# Запуск production версии
npm start
```

## 📚 Дополнительно

Для подробной информации смотрите:
- `SSR-LOCALSTORAGE-FIX.md` - детальное описание проблемы и решения
- `TIMEWEB-DEPLOYMENT-GUIDE.md` - инструкция по деплою

## 🆘 Если что-то не работает

1. Очистите кеш Next.js:
   ```bash
   rm -rf .next
   npm run build
   ```

2. Переустановите зависимости:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. Проверьте версию Node.js:
   ```bash
   node -v  # Должна быть >= 18
   ```

---
✨ **Готово! Ваше приложение теперь полностью совместимо с SSR.**

# 🔐 Memberstack Login - Инструкция по настройке и отладке

## ✅ Что было исправлено

1. **Подключена интеграция с реальным Memberstack API** вместо mock-данных
2. **Добавлено подробное логирование** на всех этапах аутентификации
3. **Форма логина теперь полностью функциональна** и делает редирект после успешного входа
4. **Добавлена обработка ошибок** с понятными сообщениями пользователю

## 📋 Пошаговая настройка

### Шаг 1: Убедитесь, что у вас есть Memberstack Public Key

1. Войдите в [Memberstack Dashboard](https://app.memberstack.com/)
2. Перейдите в **Settings** → **API Keys**
3. Скопируйте **Public Key** (начинается с `pk_`)

### Шаг 2: Настройте переменные окружения

Создайте или обновите файл `.env.local` в корне проекта:

```env
# Memberstack Configuration
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY=pk_your_public_key_here

# Admin Access (опционально)
NEXT_PUBLIC_ADMIN_USER_IDS=your_memberstack_user_id

# Database (если еще не настроено)
DATABASE_URL="your_database_url"
```

**Важно:** Для доступа в админ-панель используйте один из способов:
- Email с доменом `@admin.learnify.com` (например: `test@admin.learnify.com`)
- Или добавьте свой Memberstack User ID в `NEXT_PUBLIC_ADMIN_USER_IDS`

### Шаг 3: Создайте тестового пользователя в Memberstack

1. Войдите в [Memberstack Dashboard](https://app.memberstack.com/)
2. Перейдите в **Members** → **Add Member**
3. Создайте тестового пользователя:
   - **Email:** `test@admin.learnify.com` (для админ доступа)
   - **Password:** `Test123!` (или любой другой)
   - **Name:** Ваше имя (опционально в custom fields)

### Шаг 4: Перезапустите сервер разработки

```bash
npm run dev
```

## 🔍 Как тестировать логин с логированием

### 1. Откройте консоль браузера

Нажмите `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)

### 2. Перейдите на страницу логина

```
http://localhost:3000/login
```

### 3. Следите за логами в консоли

Вы увидите подробные логи на каждом этапе:

#### При загрузке страницы:
```
🚀 [MemberstackProvider] Initializing...
🔑 [MemberstackProvider] Public key: pk_sb_abc...
📦 [MemberstackProvider] Loading Memberstack SDK...
✅ [MemberstackProvider] Memberstack module loaded
✅ [MemberstackProvider] Memberstack initialized
✅ [MemberstackProvider] Memberstack instance attached to window
ℹ️ [MemberstackProvider] No user logged in
🔄 [useAuth] Loading user...
ℹ️ [useAuth] No user logged in
✅ [useAuth] Auth check complete
```

#### При попытке логина:
```
📝 [LoginPage] Form submitted: { isLogin: true, email: 'test@admin.learnify.com' }
🔐 [LoginPage] Attempting login...
🔐 [Memberstack] Sign in attempt: { email: 'test@admin.learnify.com' }
📤 [Memberstack] Sending login request to Memberstack API...
✅ [Memberstack] Login successful: { id: 'mem_...', email: '...', customFields: {...} }
✅ [LoginPage] Login successful, redirecting...
```

#### При успешном логине:
```
🔍 [Memberstack] Getting current user...
✅ [Memberstack] SDK instance found, fetching current member...
✅ [Memberstack] User loaded: { id: 'mem_...', email: '...', customFields: {...} }
✅ [useAuth] User loaded successfully: { id: '...', email: '...', name: '...' }
```

## ❌ Распространенные ошибки и решения

### Ошибка: "Memberstack SDK not loaded"

**Причина:** Не найден Public Key или SDK не загрузился

**Решение:**
1. Проверьте что `NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY` правильно указан в `.env.local`
2. Убедитесь что переменная начинается с `NEXT_PUBLIC_`
3. Перезапустите сервер после изменения `.env.local`

### Ошибка: "Login failed - Invalid credentials"

**Причина:** Неправильный email или пароль

**Решение:**
1. Проверьте что пользователь существует в Memberstack Dashboard
2. Убедитесь что пароль правильный
3. Проверьте что email написан без ошибок

### Ошибка: "Failed to load Memberstack"

**Причина:** Пакет @memberstack/dom не установлен

**Решение:**
```bash
npm install @memberstack/dom
```

### Логи показывают "mock" или не появляются

**Причина:** Memberstack SDK не инициализирован

**Решение:**
1. Откройте консоль браузера
2. Введите: `window.memberstack`
3. Если `undefined` - проверьте Public Key и перезагрузите страницу
4. Проверьте Network tab - должен загружаться скрипт memberstack.js

## 🎯 После успешного логина

После успешного входа:
1. ✅ Пользователь автоматически перенаправляется на `/courses`
2. ✅ Состояние авторизации обновляется
3. ✅ В шапке появляется аватар пользователя
4. ✅ Доступны защищенные маршруты

Для админов с email `@admin.learnify.com`:
- ✅ Доступна админ-панель `/admin`
- ✅ Доступны все админские функции

## 🔧 Дополнительная отладка

### Проверить текущего пользователя в консоли:

```javascript
// Получить Memberstack instance
window.memberstack

// Проверить текущего пользователя
window.memberstack.getCurrentMember().then(({data}) => console.log(data))

// Проверить конфигурацию
console.log(process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY)
```

### Проверить Network запросы:

1. Откройте DevTools → Network
2. Попробуйте залогиниться
3. Ищите запросы к `api.memberstack.com`
4. Проверьте статус ответов (должен быть 200)

## 📞 Поддержка

Если проблема не решается:

1. **Проверьте логи** - все ошибки выводятся в консоль с эмодзи-префиксами (❌, ⚠️, ✅)
2. **Проверьте Memberstack Dashboard** - убедитесь что пользователь создан
3. **Проверьте Network tab** - смотрите что приходит от API
4. **Проверьте документацию** - [Memberstack Docs](https://docs.memberstack.com/)

---

## 🎉 Готово!

Теперь ваша система аутентификации полностью настроена и работает с реальным Memberstack API. Все действия логируются в консоли для удобной отладки.

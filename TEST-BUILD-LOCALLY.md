# 🧪 Тестирование исправлений localStorage локально

## ⚡ Быстрый тест перед деплоем

Перед тем, как деплоить на TimeWeb, ОБЯЗАТЕЛЬНО протестируйте локально:

### Шаг 1: Очистите кеш и пересоберите

```bash
# Удалите папку .next
rm -rf .next

# Или на Windows PowerShell:
Remove-Item -Recurse -Force .next

# Пересоберите проект
npm run build
```

**Ожидаемый результат:**
- ✅ Сборка должна завершиться УСПЕШНО
- ❌ НЕ должно быть ошибок `ReferenceError: localStorage is not defined`
- ✅ Все страницы должны успешно pre-render'иться

### Шаг 2: Запустите production версию

```bash
npm start
```

Откройте браузер на http://localhost:3000

### Шаг 3: Проверьте критические страницы

Откройте каждую из этих страниц и убедитесь, что нет ошибок:

- ✅ http://localhost:3000/ - главная
- ✅ http://localhost:3000/courses - каталог курсов
- ✅ http://localhost:3000/dashboard - дашборд пользователя
- ✅ http://localhost:3000/wishlist - список желаемого
- ✅ http://localhost:3000/admin - админ панель
- ✅ http://localhost:3000/admin/settings - настройки админа
- ✅ http://localhost:3000/admin/courses - управление курсами
- ✅ http://localhost:3000/admin/users - управление пользователями
- ✅ http://localhost:3000/admin/analytics - аналитика

### Шаг 4: Проверьте консоль браузера

Откройте DevTools (F12) и проверьте вкладку Console:

**Не должно быть:**
- ❌ `ReferenceError: localStorage is not defined`
- ❌ `ReferenceError: window is not defined`
- ❌ `ReferenceError: document is not defined`

**Допустимы:**
- ⚠️ Предупреждения о Memberstack (если не настроен)
- ⚠️ Ошибки подключения к API (если база данных не настроена)

### Шаг 5: Проверьте работу localStorage

1. Откройте DevTools (F12) → Application → Local Storage → http://localhost:3000
2. Должен появиться ключ `learnify-user-storage` (если вы использовали какой-то функционал)
3. Данные должны сохраняться при перезагрузке страницы

## 🐳 Тест Docker сборки (опционально)

Если хотите быть на 100% уверены, протестируйте Docker сборку:

```bash
# Соберите Docker образ
docker build -t courses-app-test .

# Если сборка УПАЛА с ошибкой localStorage - исправления НЕ работают!
# Если сборка ПРОШЛА успешно - можно деплоить!
```

Если сборка прошла успешно, запустите контейнер:

```bash
docker run -p 3000:3000 -e DATABASE_URL="your-db-url" courses-app-test
```

Повторите Шаги 3-5 выше.

## ✅ Чеклист готовности к деплою

Перед деплоем убедитесь:

- [ ] `npm run build` завершается БЕЗ ошибок localStorage
- [ ] Все критические страницы открываются без ошибок
- [ ] В консоли браузера нет ошибок localStorage/window/document
- [ ] localStorage работает (сохраняет данные)
- [ ] Docker сборка проходит успешно (опционально, но рекомендуется)

## 🚨 Если тесты НЕ прошли

### Ошибка все еще есть при `npm run build`:

1. Убедитесь, что файл `src/store/userStore.ts` содержит:
   ```typescript
   export const useUserStore = 
     typeof window !== 'undefined'
       ? create<UserState>()(persist(...))
       : create<UserState>()(...);
   ```

2. Очистите node_modules и переустановите:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   npm run build
   ```

### Ошибка в браузере, но сборка прошла:

1. Проверьте, что все страницы имеют `'use client'` директиву
2. Проверьте, что нет прямых обращений к localStorage без проверок

### localStorage не сохраняется:

1. Это нормально для режима инкогнито или если localStorage отключен
2. Проверьте квоту localStorage в DevTools
3. Убедитесь, что `skipHydration: true` указан в настройках persist

## 📞 Нужна помощь?

Если проблемы остались после всех тестов:
1. Проверьте версию Node.js: `node -v` (должна быть >= 18)
2. Проверьте версию Next.js в package.json (должна быть >= 14)
3. Посмотрите полный лог ошибки и найдите файл, который вызывает проблему
4. Убедитесь, что все изменения из `SSR-LOCALSTORAGE-FIX.md` применены

---

🎯 **Если все тесты прошли - можно деплоить на TimeWeb!**

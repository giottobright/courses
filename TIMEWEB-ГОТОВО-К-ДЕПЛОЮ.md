# 🚀 Готово к деплою на Timeweb Cloud

## ✅ Что было исправлено

### 1. Добавлена явная настройка переменных окружения

**Файл:** `next.config.mjs`

Добавлен блок `env` который **гарантирует** доступность переменных на Timeweb Cloud:

```javascript
env: {
  NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY: process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY,
  NEXT_PUBLIC_APP_URL: process.env.NEXT_PUBLIC_APP_URL,
  NEXT_PUBLIC_APP_NAME: process.env.NEXT_PUBLIC_APP_NAME,
  NEXT_PUBLIC_ADMIN_USER_IDS: process.env.NEXT_PUBLIC_ADMIN_USER_IDS,
}
```

Это решает проблему когда переменные не попадают в клиентский код на Timeweb.

### 2. Создана документация для Timeweb

- ✅ `TIMEWEB-FIX-NOW.md` - Быстрое исправление (НАЧНИТЕ С ЭТОГО!)
- ✅ `TIMEWEB-MEMBERSTACK-FIX.md` - Детальная инструкция по всем вариантам
- ✅ `TIMEWEB-DEPLOYMENT-GUIDE.md` - Полное руководство по деплою (уже было)

---

## 🎯 Что делать дальше

### Шаг 1: Закоммитьте изменения

```bash
# Добавьте измененный файл
git add next.config.mjs

# Создайте коммит
git commit -m "fix: add explicit env vars for Timeweb Cloud compatibility"

# Запушьте на удаленный репозиторий
git push origin main
```

### Шаг 2: Настройте переменные на Timeweb

**Timeweb Cloud** → Ваше приложение → **Настройки** → **Переменные окружения**

Добавьте (если еще не добавлены):

```
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY = pk_ваш_ключ
NEXT_PUBLIC_APP_URL = https://ваш-домен.timeweb.cloud
NEXT_PUBLIC_APP_NAME = Learnify
DATABASE_URL = postgresql://...
```

**⚠️ ВАЖНО:** Без кавычек, просто значения!

### Шаг 3: Пересоберите на Timeweb

**ЭТО ОБЯЗАТЕЛЬНЫЙ ШАГ!**

1. Перейдите в раздел вашего приложения
2. Нажмите **"Пересобрать"** (Rebuild)
3. Дождитесь окончания сборки (5-15 минут)
4. Нажмите **"Перезапустить"** (Restart)

### Шаг 4: Проверьте работу

1. Откройте ваш сайт на Timeweb
2. Откройте консоль браузера (`F12`)
3. Обновите страницу с очисткой кэша (`Ctrl+Shift+R`)
4. Смотрите логи в консоли

**Ожидаемый результат:**
```
✅ [MemberstackProvider] Public key: pk_sb_abc...
✅ [MemberstackProvider] Memberstack initialized
```

### Шаг 5: Проверьте логин

1. Перейдите на `/login`
2. Введите тестовые данные
3. Нажмите "Sign In"
4. Должен быть редирект на `/courses`

---

## 📋 Требуемые переменные окружения на Timeweb

### Обязательные:

```bash
# База данных
DATABASE_URL="postgresql://user:password@host:port/db"

# Memberstack (для логина)
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY="pk_sb_..."

# URL приложения
NEXT_PUBLIC_APP_URL="https://ваш-домен.timeweb.cloud"
```

### Опциональные:

```bash
# Имя приложения
NEXT_PUBLIC_APP_NAME="Learnify"

# ID админов (через запятую)
NEXT_PUBLIC_ADMIN_USER_IDS="mem_id1,mem_id2"

# Stripe (если используете платежи)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_..."
STRIPE_SECRET_KEY="sk_..."

# Cloudinary (если используете загрузку изображений)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="..."
CLOUDINARY_API_KEY="..."
CLOUDINARY_API_SECRET="..."
```

---

## 🔍 Диагностика

### Проверка 1: Переменные в браузере

Откройте консоль (F12) и введите:

```javascript
console.log('Memberstack Key:', process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY);
console.log('App URL:', process.env.NEXT_PUBLIC_APP_URL);
```

**Если показывает значения** - ✅ переменные работают  
**Если undefined** - ❌ нужна пересборка

### Проверка 2: Логи инициализации

В консоли браузера должно быть:

```
🚀 [MemberstackProvider] Initializing...
🔑 [MemberstackProvider] Public key: pk_sb_...
✅ [MemberstackProvider] Memberstack initialized
```

### Проверка 3: Тест логина

1. Откройте `/login`
2. Введите email: `test@admin.learnify.com`
3. Введите пароль (созданный в Memberstack)
4. Нажмите "Sign In"

**В консоли должно появиться:**
```
📝 [LoginPage] Form submitted
🔐 [Memberstack] Sign in attempt
✅ [Memberstack] Login successful
```

---

## ❗ Частые ошибки

### Ошибка: "Public key: NOT FOUND"

**Причина:** Переменная не попала в сборку

**Решение:**
1. Добавьте переменную на Timeweb
2. **Обязательно пересоберите** проект
3. Перезапустите приложение
4. Очистите кэш браузера

### Ошибка: "Memberstack SDK not loaded"

**Причина:** Ключ не найден или неверный

**Решение:**
1. Проверьте что ключ правильный (начинается с `pk_`)
2. Проверьте название переменной: `NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY`
3. Пересоберите на Timeweb

### Ошибка: "Invalid credentials"

**Причина:** Неправильный email/пароль или пользователь не создан

**Решение:**
1. Проверьте в Memberstack Dashboard что пользователь существует
2. Убедитесь что пароль правильный
3. Попробуйте создать нового тестового пользователя

---

## 📚 Дополнительные ресурсы

### Быстрый старт:
- **`TIMEWEB-FIX-NOW.md`** - Начните отсюда!

### Детальные инструкции:
- **`TIMEWEB-MEMBERSTACK-FIX.md`** - Все варианты решения проблемы
- **`TIMEWEB-DEPLOYMENT-GUIDE.md`** - Полное руководство по деплою
- **`START-HERE.md`** - Общая инструкция по логину

### Отладка:
- **`MEMBERSTACK-LOGIN-DEBUG.md`** - Детальная отладка логина
- **`QUICK-START-LOGIN.md`** - Быстрый чеклист

---

## ✅ Финальный чеклист

Перед тем как считать деплой завершенным:

- [ ] `next.config.mjs` содержит блок `env` с переменными
- [ ] Изменения закоммичены и запушены
- [ ] Переменные окружения добавлены на Timeweb
- [ ] Проект пересобран на Timeweb (REBUILD)
- [ ] Приложение перезапущено
- [ ] Кэш браузера очищен
- [ ] Консоль показывает "Memberstack initialized"
- [ ] Логин работает и делает редирект
- [ ] Админ-панель доступна (если есть админ email)
- [ ] Курсы отображаются
- [ ] База данных подключена и работает

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение должно **полностью работать** на Timeweb Cloud с рабочим логином через Memberstack!

**Если возникли проблемы:**
1. Проверьте логи в консоли браузера
2. Проверьте логи сборки на Timeweb
3. Смотрите `TIMEWEB-MEMBERSTACK-FIX.md` для детальной диагностики

**Удачи с запуском! 🚀**

# 🔧 Исправление Memberstack на Timeweb Cloud

## ❌ Проблема

Вы видите ошибки:
```
❌ [MemberstackProvider] Public key: NOT FOUND
❌ Memberstack SDK not loaded. Please check your configuration.
```

Хотя переменные окружения настроены на Timeweb.

## 🔍 Причина

**Next.js требует особой настройки переменных для клиентской стороны:**

1. ⚠️ Переменные с `NEXT_PUBLIC_` должны быть доступны **во время сборки** (build time)
2. ⚠️ Timeweb может устанавливать переменные только для runtime, а не для build
3. ⚠️ Если переменные добавлены после сборки - они не попадут в клиентский код

## ✅ Решение

### Вариант 1: Пересборка на Timeweb (РЕКОМЕНДУЕТСЯ)

После добавления переменных окружения на Timeweb, вы **ОБЯЗАТЕЛЬНО** должны пересобрать проект!

#### Шаг 1: Проверьте переменные на Timeweb

Зайдите в панель Timeweb Cloud:
1. Откройте ваше приложение
2. Перейдите в **"Настройки"** → **"Переменные окружения"**
3. Проверьте что есть:

```bash
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY=pk_ваш_ключ
```

**⚠️ ВАЖНО:** Переменная ДОЛЖНА начинаться с `NEXT_PUBLIC_`

#### Шаг 2: Пересоберите проект

**Через панель Timeweb:**
1. Перейдите в раздел **"Сборка"** или **"Деплой"**
2. Нажмите **"Пересобрать"** или **"Rebuild"**
3. Дождитесь окончания сборки (5-10 минут)
4. Перезапустите приложение

**Или через SSH:**
```bash
# Подключитесь к серверу по SSH
ssh your_user@your_server

# Перейдите в папку проекта
cd /path/to/your/app

# Пересоберите проект
npm run build

# Перезапустите PM2
pm2 restart all
```

#### Шаг 3: Проверьте результат

Откройте ваш сайт и проверьте консоль браузера (F12):
- Должно быть: `✅ [MemberstackProvider] Memberstack initialized`
- Не должно быть: `❌ Public key: NOT FOUND`

---

### Вариант 2: Хардкод ключа (временное решение для теста)

**⚠️ Только для тестирования! НЕ используйте в продакшене с реальным ключом!**

Чтобы проверить что проблема именно в переменных окружения, временно захардкодьте ключ:

#### Измените файл `src/components/MemberstackProvider.tsx`:

**БЫЛО:**
```typescript
const publicKey = process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY;
```

**СТАЛО (временно):**
```typescript
// Временно для теста - замените на ваш реальный ключ
const publicKey = process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY || 'pk_ваш_реальный_ключ_здесь';
```

Или полностью хардкод:
```typescript
const publicKey = 'pk_ваш_реальный_ключ_здесь';
```

**После этого:**
1. Закоммитьте изменения
2. Запушьте на сервер
3. Пересоберите на Timeweb
4. Проверьте работу

**Если заработало:**
- Значит проблема была в переменных окружения
- Верните код обратно и правильно настройте переменные на Timeweb
- Обязательно пересоберите после настройки переменных

---

### Вариант 3: Создайте файл next.config.js

Иногда Timeweb требует явного указания публичных переменных в конфиге.

Создайте файл `next.config.js` в корне проекта (если его нет):

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY: process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY,
    NEXT_PUBLIC_APP_URL: process.env.NEXT_PUBLIC_APP_URL,
    NEXT_PUBLIC_ADMIN_USER_IDS: process.env.NEXT_PUBLIC_ADMIN_USER_IDS,
  },
};

module.exports = nextConfig;
```

После этого пересоберите проект на Timeweb.

---

## 🔍 Диагностика

### Проверка 1: Консоль браузера

Откройте сайт и консоль (F12), введите:

```javascript
// Проверить переменную
console.log(process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY)

// Должно показать ваш ключ (pk_...)
// Если undefined - переменная не попала в build
```

### Проверка 2: Логи сборки на Timeweb

Проверьте логи сборки на Timeweb:
1. Перейдите в раздел "Логи" или "Сборка"
2. Найдите последнюю сборку
3. Поищите в логах упоминания о переменных окружения

**Должно быть что-то вроде:**
```
Environment variables loaded: NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY=pk_...
```

### Проверка 3: SSH доступ

Если есть SSH доступ:

```bash
# Подключитесь
ssh your_user@your_server

# Проверьте переменные
printenv | grep MEMBERSTACK

# Должно показать:
# NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY=pk_...
```

---

## 📋 Чеклист исправления

- [ ] Переменная называется **точно** `NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY`
- [ ] Переменная добавлена в панели Timeweb Cloud
- [ ] После добавления переменной был сделан **REBUILD**
- [ ] После rebuild было сделано **перезапуск приложения**
- [ ] Очищен кэш браузера (`Ctrl+Shift+R`)
- [ ] Проверена консоль браузера на наличие ключа
- [ ] Проверены логи сборки на Timeweb

---

## 🎯 Пошаговое решение для Timeweb

### 1. Проверьте переменные в панели

**Timeweb Cloud** → Ваше приложение → **Настройки** → **Переменные окружения**

Убедитесь что есть:
```
Имя: NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY
Значение: pk_sb_ваш_ключ_без_кавычек
```

**⚠️ БЕЗ КАВЫЧЕК!** Просто `pk_sb_...` а не `"pk_sb_..."`

### 2. Сохраните и пересоберите

1. **Сохраните** переменные
2. Перейдите в раздел **"Приложения"** → **Ваше приложение**
3. Нажмите **"Действия"** → **"Пересобрать"** (или **"Rebuild"**)
4. Дождитесь окончания сборки (5-15 минут)

### 3. Перезапустите приложение

После сборки:
1. Нажмите **"Перезапустить"** или **"Restart"**
2. Дождитесь запуска (1-2 минуты)

### 4. Проверьте результат

1. Откройте ваш сайт
2. Откройте консоль браузера (`F12`)
3. Обновите страницу с очисткой кэша (`Ctrl+Shift+R`)
4. Смотрите логи в консоли

**Должно быть:**
```
✅ [MemberstackProvider] Memberstack initialized
🔑 [MemberstackProvider] Public key: pk_sb_abc...
```

**НЕ должно быть:**
```
❌ [MemberstackProvider] Public key: NOT FOUND
```

---

## ❓ Часто задаваемые вопросы

### Q: Я добавил переменную, но она не работает
**A:** Вы **ОБЯЗАТЕЛЬНО** должны пересобрать проект после добавления переменных! Next.js встраивает `NEXT_PUBLIC_*` переменные во время сборки.

### Q: Переменная есть на сервере, но не в браузере
**A:** `NEXT_PUBLIC_*` переменные встраиваются в код при сборке. Если вы добавили переменную после сборки - нужно пересобрать.

### Q: Сколько ждать после пересборки?
**A:** Сборка занимает 5-15 минут на Timeweb. После сборки перезапустите приложение и очистите кэш браузера.

### Q: Можно ли использовать хардкод в продакшене?
**A:** **НЕТ!** Хардкод только для тестирования. В продакшене используйте переменные окружения.

### Q: Что делать если ничего не помогает?
**A:** Попробуйте создать `next.config.js` (см. Вариант 3 выше) и пересоберите снова.

---

## 🆘 Если ничего не помогло

1. **Удалите приложение на Timeweb** и создайте заново
2. **Сразу добавьте все переменные** перед первой сборкой
3. **Запустите сборку**
4. **Проверьте результат**

Это гарантирует что переменные будут доступны с самого начала.

---

## ✅ После успешного исправления

Вы должны увидеть в консоли:

```
🚀 [MemberstackProvider] Initializing...
🔑 [MemberstackProvider] Public key: pk_sb_abc...
📦 [MemberstackProvider] Loading Memberstack SDK...
✅ [MemberstackProvider] Memberstack module loaded
✅ [MemberstackProvider] Memberstack initialized
✅ [MemberstackProvider] Memberstack instance attached to window
ℹ️ [MemberstackProvider] No user logged in
```

И логин будет работать! 🎉

<function_calls>
<invoke name="Read">
<parameter name="path">c:\Users\User\Desktop\courses\src\components\MemberstackProvider.tsx
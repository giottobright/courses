# ⚡ Быстрый старт - Тестирование логина

## ✅ Чеклист настройки (5 минут)

### 1️⃣ Установите зависимости (если еще не сделано)
```bash
npm install
```

### 2️⃣ Создайте `.env.local` файл в корне проекта

```env
DATABASE_URL="ваш_database_url"
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY=pk_ваш_ключ_из_memberstack
```

**Где взять ключ?**
- Откройте https://app.memberstack.com/
- Settings → API Keys → скопируйте Public Key

### 3️⃣ Создайте тестового пользователя в Memberstack

**Вариант А (для админа):**
- Email: `test@admin.learnify.com`
- Password: любой (минимум 6 символов)

**Вариант Б (обычный пользователь):**
- Email: любой
- Password: любой (минимум 6 символов)

### 4️⃣ Запустите сервер
```bash
npm run dev
```

### 5️⃣ Тестируйте логин

1. Откройте http://localhost:3000/login
2. Откройте консоль браузера (`F12`)
3. Введите email и пароль
4. Нажмите "Sign In"

## 🔍 Что проверять в консоли?

### При загрузке страницы должно быть:
```
🚀 [MemberstackProvider] Initializing...
✅ [MemberstackProvider] Memberstack initialized
```

### При логине должно быть:
```
📝 [LoginPage] Form submitted
🔐 [Memberstack] Sign in attempt
✅ [Memberstack] Login successful
✅ [LoginPage] Login successful, redirecting...
```

### После успешного логина:
- ✅ Редирект на `/courses`
- ✅ В шапке появляется ваш аватар/имя
- ✅ Доступны защищенные страницы

### Для админов:
- ✅ Доступна страница `/admin`
- ✅ Показывается админ панель

## ❌ Если что-то не работает

### Проблема: "Memberstack public key not found"
**Решение:** Проверьте что в `.env.local` есть `NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY` и перезапустите сервер

### Проблема: "Login failed - Invalid credentials"
**Решение:** Проверьте что пользователь создан в Memberstack Dashboard

### Проблема: Ничего не происходит при нажатии кнопки
**Решение:** Откройте консоль браузера - там будут подробные логи ошибки

### Проблема: Нет логов в консоли
**Решение:** 
1. Убедитесь что консоль открыта ДО загрузки страницы
2. Перезагрузите страницу с `Ctrl+Shift+R` (очистка кэша)

## 🎯 Следующие шаги

После успешного логина:
1. Попробуйте зайти на `/courses` - должен быть доступ
2. Попробуйте зайти на `/admin` (если админ) - должна открыться панель
3. Попробуйте выйти и снова войти

---

**Нужна помощь?** Смотрите полную документацию в `MEMBERSTACK-LOGIN-DEBUG.md`

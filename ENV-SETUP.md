# 🔧 Настройка переменных окружения

## Создайте файл `.env.local` в корне проекта

```env
# Database (обязательно)
DATABASE_URL="your_database_url_here"

# Memberstack Configuration (обязательно для логина)
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY=pk_your_public_key_here

# Admin Access (опционально)
# Укажите Memberstack User ID для админ доступа
NEXT_PUBLIC_ADMIN_USER_IDS=mem_your_user_id_here

# App URL (опционально, по умолчанию localhost:3000)
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

## Где взять Memberstack Public Key?

1. Войдите на https://app.memberstack.com/
2. Перейдите в **Settings** → **API Keys**
3. Скопируйте **Public Key** (начинается с `pk_`)
4. Вставьте в `.env.local`

## Как получить доступ в админ-панель?

### Вариант 1: По email домену (проще для тестирования)
Создайте пользователя в Memberstack с email: `test@admin.learnify.com`

### Вариант 2: По User ID
1. Создайте пользователя в Memberstack
2. Найдите его ID в Memberstack Dashboard (начинается с `mem_`)
3. Добавьте ID в `.env.local`:
   ```
   NEXT_PUBLIC_ADMIN_USER_IDS=mem_abc123xyz
   ```

## После настройки

1. Перезапустите сервер: `npm run dev`
2. Перейдите на `/login`
3. Войдите с учетными данными из Memberstack
4. Проверьте консоль браузера (F12) для отладочных логов

**Важно:** Переменные с префиксом `NEXT_PUBLIC_` доступны в браузере и должны перезапускаться при изменении!

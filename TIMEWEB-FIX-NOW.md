# ⚡ Быстрое исправление Memberstack на Timeweb

## 🎯 Что нужно сделать СЕЙЧАС

### 1. Проверьте переменные на Timeweb

Зайдите в **Timeweb Cloud** → Ваше приложение → **Настройки** → **Переменные окружения**

Должна быть переменная:
```
Имя: NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY
Значение: pk_sb_ваш_ключ (БЕЗ КАВЫЧЕК!)
```

**❗ ВАЖНО:**
- Название ТОЧНО `NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY`
- Без кавычек, просто `pk_sb_...`
- Если нет - добавьте прямо сейчас

### 2. Закоммитьте изменения

Я добавил явное указание переменных в `next.config.mjs`. Закоммитьте это:

```bash
git add next.config.mjs
git commit -m "fix: add explicit env vars for Timeweb Cloud"
git push origin main
```

### 3. Пересоберите на Timeweb

**⚠️ ЭТО КРИТИЧЕСКИ ВАЖНО!**

После добавления переменных окружения:

1. Перейдите в Timeweb Cloud → Ваше приложение
2. Найдите кнопку **"Пересобрать"** или **"Rebuild"** 
3. Нажмите и дождитесь окончания (5-15 минут)
4. После сборки нажмите **"Перезапустить"**

### 4. Очистите кэш браузера

```
Ctrl + Shift + R  (Windows/Linux)
Cmd + Shift + R   (Mac)
```

### 5. Проверьте результат

Откройте консоль браузера (F12) и обновите страницу.

**Должно быть:**
```
✅ [MemberstackProvider] Public key: pk_sb_abc...
✅ [MemberstackProvider] Memberstack initialized
```

**НЕ должно быть:**
```
❌ [MemberstackProvider] Public key: NOT FOUND
```

---

## 🔍 Проверка через консоль браузера

Откройте консоль (F12) и введите:

```javascript
console.log(process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY)
```

**Если показывает ваш ключ (pk_...):**
- ✅ Все работает!

**Если показывает undefined:**
- ❌ Переменная не попала в сборку
- → Пересоберите проект на Timeweb

---

## 💡 Почему это нужно?

**Next.js встраивает `NEXT_PUBLIC_*` переменные во время сборки (build time), а НЕ в runtime!**

Это значит:
1. ❌ Просто добавить переменную на сервере недостаточно
2. ✅ После добавления переменной **ОБЯЗАТЕЛЬНО** нужна пересборка
3. ✅ Переменные должны существовать ВО ВРЕМЯ сборки

---

## 🆘 Если не работает

### Вариант 1: Временный хардкод (для теста)

Измените `src/components/MemberstackProvider.tsx`:

```typescript
// БЫЛО:
const publicKey = process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY;

// СТАЛО (временно):
const publicKey = 'pk_sb_ваш_реальный_ключ';
```

Закоммитьте, запушьте, пересоберите на Timeweb.

**Если заработало** - значит проблема точно в переменных окружения.

**Верните код обратно** и правильно настройте переменные.

### Вариант 2: Проверьте логи сборки

На Timeweb:
1. Перейдите в раздел **"Логи"** или **"Логи сборки"**
2. Найдите последнюю сборку
3. Проверьте что переменная видна во время сборки

---

## 📚 Полная документация

- **Детальная инструкция:** `TIMEWEB-MEMBERSTACK-FIX.md`
- **Общая настройка:** `START-HERE.md`

---

## ✅ Чеклист

- [ ] Переменная `NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY` добавлена на Timeweb
- [ ] Закоммитил изменения в `next.config.mjs`
- [ ] Запушил на GitHub/GitLab
- [ ] Пересобрал проект на Timeweb (REBUILD)
- [ ] Перезапустил приложение
- [ ] Очистил кэш браузера
- [ ] Проверил консоль - ключ отображается
- [ ] Логин работает!

---

**После выполнения всех шагов логин должен заработать! 🚀**
